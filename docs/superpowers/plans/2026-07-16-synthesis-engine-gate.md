# Synthesis Engine Gate — ADR-G005 (founder-review spec; SPEC ONLY, NO CODE)

**Status:** 🟡 **S-0 BOARD SIGNED (founder, 2026-07-16) — S-0 BUILD AUTHORIZED. S-1 board still unruled; NO S-1 code.** Rulings on record: **SO-S0-1 APPROVED** (one `certifySynthesisForVerdict()` upstream of EVERY entry into computeVerdict — pipeline stageVerdict, admin viewmodel, rejudge, and inside the composition before applyDocumentationNoOverride's recompute; ceiling pattern, all sites or it doesn't ship; computeVerdict byte-identical). **SO-S0-2 APPROVED WITH A SPLIT — the two conditions are NOT the same test:** `critical` (locks do_not_rely) requires resolving evidence_ids to real M1 items + ≥2 DISTINCT tracks + ≥1 side certainty "verified" (fail → clamp "high" + audit); `is_load_bearing` (floors VBP at ≥2) requires resolving evidence_ids ONLY — no track-diversity bar (fail → coerce false + audit). Rationale on record: critical locks the harshest verdict; devastating single-track findings already reach do_not_rely via track-level pure vetoes certified at the right layer; a single-track contradiction still counts as load-bearing and still floors. **SO-S0-3 APPROVED** — enum {low|medium|high|critical}, downward-only clamping, m4c origin cap preserved through the merge (a track5_m4c record can NEVER mint critical or is_load_bearing:true by copy-through), m4c-1.1.0 additive with `origin`, Track 5's writer stays 1.0.0, reader accepts both. **HARD RULE:** m4c "high" must NOT map to "critical" — Track 5 is non-voting (AT-B1) and the merge is the exact act that could un-freeze it; BOTH certified fields carry the cap. **SO-S0-4 APPROVED AND HARDENED** — doubt structurally locked out of the verdict; G005's doubt-feeds-verdict clause is DEAD (frozen wins); freeze condition recorded verbatim in the tracker. **SEQUENCE RULED: S-0 → S-2 → S-1** (S-2 does NOT fold into S-1 — declined; pins and the ios model string land BEFORE the big build). **✅ S-0 FROZEN — founder-declared 2026-07-17 at `11476f7` on staging. FULL AT RECORD: AT-S0-1 PASS (poisoned byte-identity + positive control). AT-S0-2 PASS — ZERO certification audits across all four delivered cases; 3/4 reproduce exactly (AWI-2607-021 attempt 6 usable_with_conditions — the natural canary fixture, never actually tested until the rejudge fix; AWI-2607-023 attempt 2 VBP; AWI-2607-031 attempt 3 VBP). AWI-2606-001 attempt 1 (stored source_clear vs rejudged do_not_rely) RULED PRE-EXISTING, NOT S-0 — reasoning on record: the mismatch runs TOWARD severity while certification only clamps DOWNWARD (SO-S0-3, test-locked), so S-0 is structurally incapable of causing it; zero audits, vetoes:none, all four tracks n_a; AT-S0-1 proves empty-in→empty-out is identity and every delivered case carries stub synthesis, so certification is provably a no-op on the whole delivered corpus; the all-n_a→do_not_rely semantics changed at H3, months before S-0; the two-commit test was ruled unnecessary — the structural proof is stronger than a sample of one. AT-S0-3 PASS · AT-S0-4 PASS (derived scan, RED-proven twice) · AT-S0-5 PASS (686/686 unpiped, VALIDATION 1.7.0 in the certification commit). CARRIED FORWARD VERBATIM (SO-S0-4 freeze condition): "any future attempt to make doubt a verdict input is its own founder-gated spec with its own certification. Never a rider. Never a follow-on commit."**
**✅ S-2 FROZEN — founder-declared 2026-07-17 at `acef731` on staging. FULL AT RECORD: full suite 697/697 across 83 files, run UNPIPED, `$LASTEXITCODE` 0 (686 + 11 reconciles; +2 test files) — the targeted 11-test run (s2.locks + versionDelta) SUBSUMED by the full suite. Both `synthesis_version` pins verified LIVE BY THE FOUNDER in `rerun-batch.ts` and `dispute-rerun.ts` at `"0.0.0"` with STOP semantics. The live replay half of (c) DEFERRED — ruled: it costs a replay and appends an attempt to a frozen-pointer delivered case for a sample of one; A5's backtest exercises the version delta 36 times on the corpus that matters. (a)–(d) accepted as built; (c) accepted with the founder's principle honored — the replay preflight ATTRIBUTES, never hard-blocks; rerun-batch keeps its STOP (money under changed rules).**
**TWO ITEMS LOGGED AT THE S-2 FREEZE (founder, 2026-07-17):**
**(i) S-2 (e) — ACCEPTED AS THE DERIVED CLIENT-BOUNDARY LOCK; the npm `server-only` mechanism DECLINED** (founder's stated reason: it throws on plain-Node import, so every founder script and all test files would need `--conditions=react-server` churn, and a tsconfig alias would defeat the Next protection that is the point; the constraint was flagged, not built around — right call). **LOGGED GAP, recorded verbatim: the lock is NOT TRANSITIVE.** It scans "use client" files for runtime imports of `@/lib/research`. A client component importing `./helper`, where `helper.ts` imports `verdictEngine`, passes GREEN and ships the engine to the browser — Next's "use client" boundary is transitive, the lock is not, and `computeVerdict` is pure math with no Node APIs so nothing else objects. That is the PG-1/N4 leak class. Current state verified clean (only `import type` crosses — erased). Defense-in-depth behind F2's allowlist, not the primary guard. **CLOSING THE TRANSITIVE GAP IS A HARD ENTRY CONDITION ON THE CLIENT-SURFACE GATE** — that is exactly when "use client" files multiply and someone writes that helper.
**(ii) S-1 CHECKLIST REQUIREMENT (logged at the freeze so it is not remembered later): `synthesis_version` "0.0.0" is an INERT PLACEHOLDER. S-1 MUST bump it — and because the pin is a FORWARD pin, the moment S-1 lands, `rerun-batch` and `dispute-rerun` BOTH STOP WORKING until their pins are updated in the same commit. Fail-loud and correct — but it goes on S-1's checklist, not on the day as a surprise.**
**✅ S-1 BOARD FULLY SIGNED (founder, 2026-07-17, spec read in full at `8a59011`): SO-S1-1 SIGNED WITH TWO CONDITIONS, both executed same day — Condition 1: M4's In widened to carry the M1 record, provenance stated FROM SOURCE with the transaction-granularity gap flagged as OQ-S5 (see B2/M4); Condition 2: M7's gap-axis HONESTY LABEL (see B2/M7 + the skeleton — primarily-but-not-wholly deterministic; the safety property is the S-0 lock, not input purity) · SO-S1-2 SIGNED (four-call staging; B′ correctly before certification) · SO-S1-3 SIGNED · SO-S1-4 SIGNED (derivation rule executed). The M2 firewall-outcome law ✅ RULED APPROVED (arrived attached to the A1 threading — scoping reasoning on record at B2/M2). OQ-S1 ✅ RULED (a); A2 unblocked (founder authors the matrix). S-1 REMAINS BLOCKED ON THE FOUNDER: OQ-S4 (blocks M9) · the doubt-matrix fill (blocks M7). **OQ-S5 ✅ RULED (2026-07-17, neither flagged option): conflict-merge half CODE-ENFORCED at vendor-document-class granularity — at most one load-bearing record from document-sourced contradictions; precondition verified from source (claimant is a code literal at every construction site); two-sided obligation spec'd; residual noted, stays open as a note (see B2/M4). ROUND-3 CORRECTIONS EXECUTED (founder, 2026-07-17): (1) the principle NARROWED on the record — the cap's subject is ARTIFACTS OF ONE ACT OF CLAIMING (submitted paperwork), not "one claimant is one voice" (the founder's own over-broad first phrasing, corrected so the words never license widening the cap); (2) the claimant⟺track4 equivalence LOCKED not claimed — S-1 derived source-scan lock, no site outside track4 may write `claimant: "vendor"`, fails by name, AT (iv); (3) M2's attribution fields RENAMED before they exist (`claim_attributed_to` / `attributed_party_benefits`) with the naming law in both contracts — M1 carries provenance CLASS, M2 carries per-claim attribution; the `elevated`-collision lesson applied to a verdict-adjacent field.** A4 stays HELD and does not block. A5 stands as the S-1 FREEZE CONDITION. NO S-1 CODE.**
**ROUND 4 (2026-07-17, docs + a read-only data pull): the DOUBT-LEVEL OUTPUT CONTRACT is now stated in B2/M9 (shape per value + the intensity-ladder enum reading — PROPOSED, build-thread-authored on founder order; founder confirms with the matrix fill; enum unchanged) · THE MATRIX IS TIER-INDEPENDENT recorded in B2/M7 + the skeleton (one matrix, every tier; the method has no tiers) · single_99(b) homed as OQ-S6 (tier-design, ruled with the B3 map; NOT blocking S-1 — B3 handles it structurally; options cited from the tracker, none invented) · the 36-case corpus pull delivered read-only (65 attempt rows incl. junk/seed fixtures; no cell filled, no grouping suggested): `2026-07-17-doubt-matrix-corpus-pull.md` + `scripts/doubt-matrix-corpus-pull.ts`. ⚠ REQUIRED BEFORE ANY S-1 CODE: SO-S1-1's signature is RE-AFFIRMED by the planning thread against the FINAL spec in ONE read — B2 has moved substantially since `8a59011`.**
**⛔ STOP-THE-LINE ON THE MATRIX (2026-07-17, investigation complete — report: `2026-07-17-corroboration-gate-investigation.md`; REPORT ONLY, no ruling text below has been altered): zero corroboration-gate rejections across the corpus was investigated from source + census. Persistence TRUE (970 proposals / 470 gated rejections stored per attempt — A1's record class is populated); live gate CORRECT post-H7; corpus CLEAN (one corroboration-required proposal ever, died at grounding); and a STRUCTURAL POLARITY finding: CORROBORATION_REQUIRED = the 7 fraud/veto keys only, so corroboration deaths measure uncorroborated allegations AGAINST the vendor, not the vendor's unverified claims. A1's "gap MEASURED" premise, SO-S1-1 Condition 2's "primarily deterministic" label, and the skeleton's R definition all await the PLANNING THREAD's ruling; Axis 1 of the matrix fill is blocked on that ruling, not on the founder. Axis 2 unblocked.**
**→ A1 SPLIT-RULED (founder, 2026-07-17; full record in ADDENDUM-1 beside A1): the WIDENING STANDS (Move 2 intact — 970 proposals/470 gated refusals, uncopyable); the GAP MECHANISM IS DEAD (R's corroboration-death definition: empty + wrong polarity); FOUNDER PRINCIPLE RULED: R measures the vendor's OWN load-relevant story we could not observe, never allegations against them. ENUMERATION DELIVERED for the replacement ruling (`2026-07-17-r-axis-enumeration.md`): the unambiguous accepted claimed-not-observable class is ONE key (`claims_authorization_unverified`, accepted 2× ever — a finding); rejected-positive = 334/470 with a MEASURED 25% same-row re-proposal confound + the proposal≠vendor-claim conflation named; unknowns rich (0–14/attempt) but LLM-written; weight_validation era-conditional on the stored corpus (134/260 rows), unconditional current. R'S REPLACEMENT = PLANNING-THREAD RULING; nothing proposed by the build thread. Axis 1 blocked on it; Axis 2 unblocked; S-0 lock unaffected-by-design.**
**→ THE RULED CANDIDATE (CLAIMANT PROVENANCE) SIZED 2026-07-17 (`2026-07-17-claimant-provenance-sizing.md`): DOES NOT DISCRIMINATE — vendor-claimant = 0 on 62/65 attempts (only AWI-2607-031, the documents case, is non-zero); structural, not just corpus poverty: claimant "vendor" is Track-4-only, so web-borne vendor assertions are invisible to it on any corpus. Supplementary measured fact: `source_type: "vendor_self_assertion"` (stored, code-classified, covers web items) = 56 items across 27/65 attempts. Honest hole named: volume ≠ uncorroborated. A2's premise (a deterministic gap signal at useful volume exists today) is DISPROVEN on this corpus — on the founder's record. LIVE RULING: the founder's honest fallback (Axis 1 LLM-derived, truthfully labeled, S-0-bounded, G4-tuned) vs any use of the source_type fact — planning thread rules. The dealer_page routing finding logged in the tracker as a TRACK-LAYER item (corrected to Track 2 — planning-thread error owned).**
**→ FOURTH AND LAST CANDIDATE (source_type) — Q1 CLEARED + Q2 SIZED 2026-07-17 (`2026-07-17-source-type-sizing.md`). PRE-COMMITMENT RECORDED (founder): no fifth hunt — if source_type fails, Axis 1 ships LLM-derived (M3 unresolved + unknowns), truthfully labeled, S-0-bounded, G4-tuned; four candidates killed on the record IS the finding. Q1: source_type is CODE-CLASSIFIED end to end (buildProvenance → classifySource deterministic host/domain rules, vendor-domain match via resolved_domain ?? vendor_website → sourceTypeFor fixed map; the LLM never writes it; two caveats named: metadata-conditional vendor-recognition, LLM-mediated item→source linkage). Q2: UNLIKE the prior three — 27/65 attempts carry a defined ratio with spread 0.11→1.50; 51 of 56 asserted items are WEB-borne (fixes claimant's blindness); zeros are MEASUREMENTS not blindness (asserted=0 on 38/65, 13 of those zero-item); measured intra-case attempt variance (same vendor 0.29→1.00 across attempts — acquisition-luck-sensitive denominator); conflation bounded in DIRECTION by the data (observed side rises with corroboration), magnitude on adversarial cases unmeasured; in-enum on all 500 items, zero defaulted. THE RULING — source_type carries Axis 1 vs the pre-committed fallback — IS THE PLANNING THREAD'S; nothing further hunted.**
**→ FALLBACK FIRED — FOUNDER-CONFIRMED AND EXECUTED (2026-07-17): Axis 1 ships LLM-DERIVED (M3 `unresolved` + stored unknowns), truthfully labeled — the ruled text now lives in B2/M7 (Condition-2 label superseded in place) and the skeleton (Axis-1 header + R definition RULED; the moat-survives reasoning and A2-premise-disproven recorded there). The founder's own error recorded plainly beside it (his order): the matrix CELLS were fillable throughout — R's definition determines thresholds, never cells; the ruled fill order was cells-now/thresholds-at-G4; four rounds of "the fill is blocked" was a blocker asserted without checking what it actually blocked. NO FIFTH HUNT.**
**→ OQ-S4 ✅ RULED (i) AND RECORDED (founder, 2026-07-17 — the ruling was made same day but NEVER SENT until now; process note at the OQ entry: a ruling that exists only in conversation is not a ruling, the chat-only class, planning-thread error): one case verdict (frozen), M9 names the driving brand + carries the per-brand status. M9 UNBLOCKED — with ONE NEW DESCRIBE-AND-STOP: the per-brand status needs an ADDITIVE FIELD (structured data cannot ride prose); candidate shape + two candidate homes described at B2/M9, both contract changes to admin-read shapes — FOUNDER RULES home + payload semantics before S-1 writes it. Option (ii) re-homed to the client-surface/PDF gate (report design, beside client-value #6). S-1 NOW BLOCKED ON: the founder's matrix fill · the brand_status carrier ruling (new, small) · the planning thread's one-read SO-S1-1 re-affirmation against the final spec.**
**→ brand_status HOME + SEMANTICS RULED, PAYLOAD STOPPED ON VERIFICATION (founder + build thread, 2026-07-17): home = SynthesisOutput sibling (name PROPOSED `brand_evidence_status`; S-0 rebuild-safety CONFIRMED from source — certification rebuilds from emptyVerdictInput, unknown siblings can never reach the verdict); semantics = three states, not-assessed NEVER collapses to clearance (B3's law at brand granularity — else AWI-2606-001 rebuilt per-brand). VERIFICATION (a)–(d) DELIVERED (`2026-07-17-brand-status-carrier-report.md`): (a) NO code-written per-brand attribution survives to the frozen record — the per-brand query scoping dies at the pack boundary (RawSource has no question/brand field; metrics per-plugin); the only stored attribution is EvidenceItem.brand — LLM-WRITTEN, roster-unvalidated (a hallucinated brand string would store as-is); (b) the roster IS code-side (cases.brands_submitted); (c) assessed/not-assessed is deterministic PER TRACK (H3 cause flags) but NOT per brand within a ran track — assessed-clean vs not-assessed collapses without trusting LLM tags → SAID-AND-STOPPED per the order; (d) honest label: LLM tags cannot move the verdict (case-level, vetoes pool pre-tag) but CAN put the wrong brand in the client's sentence. Payload options enumerated (split-authorship labeled + roster-validation lock · frozen-pack linkage change at its own gate — EvidencePack is a FROZEN CONTRACT, flagged · dimension-granularity-only v1). FOUNDER RULES THE PAYLOAD. S-1 blocked on: matrix fill · payload ruling · SO-S1-1 one-read.**
**→ MATRIX FILLED + brand_status RULED (founder, 2026-07-17): THE FOUNDER'S LAW recorded at the skeleton top + B2/M7 + M9's rendering ("doubt is calibrated trust in a stated conclusion; confusion is the absence of one" — every cell must be actionable). Grid FILLED AND CONFIRMED, d7-1.0.0 (cells + two governing principles + per-row rationale as admin-only method + the G005-corners sanity check; G4 flag on record: `severe` never differs from `significant` in v1 — tuning question, not a blocker). Axis-2 level MEANINGS drafted PROPOSED in the skeleton (unpacked from the ruled inputs; founder confirms; thresholds blank for G4). SKELETON BUG FIXED: the superseded "primarily deterministic" honesty label now marked IN PLACE + the S-side fact stated (certainty is LLM-written — BOTH sides of Axis 1 LLM-gated; consistent with the fallback label). brand_status CORRECTED BY RULING: TWO states not three (the founder's own three-state ruling collided with never-render-as-clearance on a record that cannot distinguish assessed-clean from not-assessed; asymmetric shape — adverse_findings_attributed / no_adverse_findings_attributed, absence never clearance — ADR-G004's law at brand granularity); name accepted; ROSTER LOCK unconditional; deterministic dimensions-ran fact rides separately (PROPOSED `dimensions_assessed`); driving kept, labeled LLM-attributed; (β) declined-for-this-gate, recorded as the future path. S-1 NOW BLOCKED ON: the payload spec's return-for-confirmation (this round's B2/M9 text) · SO-S1-1's one-read re-affirmation against the final spec.**
**→ ✅ SO-S1-1 RE-AFFIRMED IN ONE READ OF THE FINAL SPEC (founder, 2026-07-17; 224 lines, pricing grep clean; per-module verification recorded in the ruling; the correction discipline held throughout — dead text preserved and marked, never deleted). THE FULL S-1 BOARD IS SIGNED: SO-S1-1..4 ✅ · OQ-S1 ✅(a) · OQ-S4 ✅(i) · OQ-S5 ✅ · Q3 ✅ · Q4 ✅(b) · matrix ✅ d7-1.0.0 · Axis-2 meanings ✅ confirmed · brand_evidence_status payload ✅ confirmed. A4 HELD non-blocking; OQ-S2/S3/S6 tier-design/client-surface, not S-1. ONE NAME RULING EXECUTED SAME DAY (the dimensions_assessed collision — ruled cause-preserving; `dimension_run_record` PROPOSED with the cause-set verification: causes separate cleanly via two code mechanisms, AND exceed the ruled three states — nothing_to_review + not_implemented flagged, not silently mapped; founder confirms shape + name). S-1 CARRIES: A1 (widening only — gap mechanism dead, fallback rules) · A2 · A3 (four calls incl. B′) · A5 · A6 · A7 · A8 · R2 · the four routed module laws · the derivation rule · OQ-S5's cap + claimant-literal lock · brand_evidence_status + roster lock + dimension_run_record. FREEZE CONDITION: A5's backtest runs over the frozen corpus and THE FOUNDER SEES THE DATA before the prompts freeze — non-negotiable. FOUNDER-OWNED PRECONDITION: AWI-2607-022's clean re-run before A5 replays the corpus. BUILD SEQUENCE PROPOSED BELOW (sequence section) — NOT STARTED; the founder rules the cut.**
**→ ✅ dimension_run_record CONFIRMED (founder, 2026-07-17 — shape and name; state/cause separation correct; the cause-selects-the-law mapping in M6's contract IS the ruling; five causes recorded, nothing_to_review's third-sentence status per OQ-A3 preserved) · ✅ SEQUENCE RULED — SIX SITTINGS (S-1a contracts-only split out; A7 injection in BOTH S-1c and S-1d; hard rule: every sitting stops before its ATs — founder runs, planning thread declares, Fable records). 🟢 S-1a OPENED (founder order, 2026-07-17): the nine contract types only, types + shape locks, zero logic — STOPS BEFORE ITS ATs.** **🟡 S-1a BUILT at `530881b` (TDD RED-first, 5 observed failures → GREEN; suite 710/710 unpiped exit 0; tsc/eslint/build green; ONLY contracts.ts + the new lock-test file touched — frozen surfaces byte-identical; design note for the AT read: SynthesisOutput's legacy module fields left loose BECAUSE the frozen S-0 lock test constructs them bare — the nine typed contracts assign INTO them one-way, all stored additions optional siblings). AWAITING FOUNDER ATs.** **✅ S-1a FROZEN — founder-declared 2026-07-18 at `530881b` on staging. FULL AT RECORD: contract locks 13/13 (BOTH seam locks green — the legacy stub still satisfies SynthesisOutput; DecisionSnapshot's five fields exact) · suite 710/710 across 84 files, UNPIPED, $LASTEXITCODE 0 (697+13) · tsc exit 0 · diff 6f42ec6..530881b exactly 2 files, both lib/research/, no frozen file named. KEEPA QUESTION A CLEAR — no reserved seam, nothing owed to S-1a's contracts; the three dormant Keepa weight keys stay flagged UNRULED for the Keepa gate (remove + rubric bump, never revive — ruling Q-K1 kills that design). The nine contract types are FROZEN — every later sitting consumes them as law. NEXT: S-1b (the M1 assembler) OPENS ON THE FOUNDER'S WORD — not before.** **🟡 S-1b BUILT at `bd5402c` (TDD; the hash conscience WATCHED-FAIL on record — a deliberate wrong projection fed rejections into the hash, the byte-identity property failed NAMING the leak with 3 distinct hashes across 4 extension variants, the no-drift lock caught it independently, then the correct separation went GREEN 11/11; suite 721/721 unpiped exit 0; tsc/eslint/build green; 2 new files only — frozen core untouched; no migration). AWAITING FOUNDER ATs — planning thread declares.** **✅ S-1b FROZEN — founder-declared 2026-07-18 at `bd5402c` on staging. FULL AT RECORD: M1 conscience 11/11 (hash byte-identity + accepted-path no-drift both green BY NAME; the RED watched-fail PROVEN — the wrong projection produced 3 distinct hashes across 4 extension variants, caught by name) · suite 721/721 across 85 files, UNPIPED, $LASTEXITCODE 0 (710+11) · tsc 0 · diff 24ae62f..bd5402c exactly 2 files, both lib/research/m1Assembler.*, no frozen file · s1a.contracts 13/13 green under the new consumer. The M1 assembler is FROZEN — the determinism anchor stands; the widened record rides beside the hash as law. NEXT: S-1c (Call A — M2+M3, the first LLM boundary) OPENS ON THE FOUNDER'S WORD — not before.**
**S-0 FREEZE-RECORD LABELING ADDENDUM (founder, 2026-07-17 — FREEZE INTACT; no code, no tests touched; NOT a defect, NOT a re-freeze): SO-S0-2's structural-critical conditions include "≥1 side certainty `verified`" — and `certainty` is LLM-WRITTEN (track-LLM output, enum-validated only; surfaced at the Axis-1 authorship verification; NOT on the record when SO-S0-2 was ruled, the founder included). WHY S-0 DOES NOT REOPEN, verbatim reasoning: the SYNTHESIS LLM cannot write certainty. M1 is code. Certainty is written by the TRACK LLM at acquisition and FROZEN before synthesis runs. S-0's actual thesis — a synthesis narrative cannot reach the verdict uncertified — is INTACT, and the poisoned property test proves exactly what it claims. What is overstated is "structurally earned": the honest label — evidence_ids resolution = CODE · ≥2 distinct tracks = CODE (`source_track` VERIFIED code-written 2026-07-17: `track_key` is a code literal at every builder return site, no LLM schema carries a track field, `normalizeEvidence` stamps it in code) · ≥1 `verified` item = FROZEN TRACK-LLM OUTPUT. Two of three conditions deterministic; the third frozen-model-written. Honest-label fix, the Condition-2 class.**
**Prior status: 🟡 S-0 BUILT at `88de6d8` (TDD, 675/675, VALIDATION 1.7.0 + pins) — AWAITING FOUNDER ATs S0-1..5; the planning thread declares the freeze. ADDENDA 1/2 RULED (founder 2026-07-16 — rulings verbatim in their files): A1/A2/A3/A5/A6/A7/A8 approved (A5 upgraded to an S-1 FREEZE CONDITION; A3 re-ratifies SO-S1-2 at FOUR calls; OQ-S1 now BLOCKING), A4 HELD; ADDENDUM-2 Move 1 rejected-as-written / reduced form approved, Move 6a → S-2 by founder override. S-2 SCOPE (ruled): ios model string from MODEL_CONFIG · synthesis_version pins · replay-attempt.ts version-pin preflight (R1 — precondition to A5) · PRICE_IN/OUT from MODEL_CONFIG (R3) · `server-only` on lib/research/** (Move 6a). S-1 additionally carries R2 (persist schema_fallback per synthesis stage) and the FOUR routed module laws now recorded in B2/OQ-S4 below.**
**Original status line:** 🔴 DRAFT — AWAITING FOUNDER REVIEW. NO CODE UNTIL RULED (per sub-gate).
**Sources read in full for this spec:** ADR-G005 (all 537 lines) · ADR-G006 · ADR-G004 master spec (`hyprriq_intelligence_engine_master.md` v2.0) · frozen `verdictEngine.ts` / `verdictCeiling.ts` / `verdictNoOverride.ts` · m4c-1.0.0 (`contracts.ts`) · F2 allowlist (`lib/data/cases.ts`) · `synthesisEngine.ts` stub + `lib/data/synthesis.ts` + admin `case-review.tsx` rendering · the tracker.
**Standing law inherited whole:** LLM proposes, code decides · frozen core byte-identical (computeVerdict, deriveTrackSignal, weights, identityResolver) · delivered records immutable (H1) · absence ≠ fraud; never "confirm authorization"; report-they-decide-never-guarantee; infra failure is never a claim about the supplier · Modules 1–8 never reach any external party · no threshold tuning to force an AT — stop and return near-miss data · Rider 2 (the secondary-path leak class) governs every narrative field.

---

# PART A — PRE-SPEC FINDINGS (answered from source, not memory)

## A1 — The stub scaffold vs G005's contract

**What the admin review page renders today** (via `buildVerdictViewModel` → `case-review.tsx`):
- **"Executive Intelligence Summary" (Module 9):** `headline`, `leading_interpretation`, `the_real_risk`, "What to Verify", "What to Monitor". **Divergence:** "What to Verify" is populated from **`module_8_vendor_questions`, NOT `module_9.what_to_verify`** — a founder-specified mapping in the viewmodel; `module_9.what_to_verify` exists in the contract but is dead weight (the vm test says "ignored — UI uses Module 8").
- **"Cross-Track Intelligence (Modules 4/5/7)":** contradictions (risk_level chip — literal `"critical"` renders red — + load-bearing marker), hypotheses — **only `what_would_change_the_leader` renders; the hypotheses array itself is never shown** (the committed leading hypothesis surfaces only indirectly via Module 9's `leading_interpretation`), and doubt calibration (`doubt_level`/`doubt_focus`/`rationale`).
- **Never rendered anywhere, admin included:** Modules 1, 2, 3, 6.

**What data shape it expects:** the `SynthesisOutput` in `contracts.ts`. Plainly: **this is a deliberately thin stage-1 scaffold, not a faithful encoding of G005's contract.** Field-level divergences:
1. **Modules 1/2/3/6 are typed `unknown[]`** — no contract exists. G005 defines full shapes (normalized evidence item; claim attribution `{claim, claimant, claimant_benefits, corroboration, weight}`; assertion `{assertion, status, supporting_evidence, contradicting_evidence, confidence}`; gap `{unknown, why_it_matters, is_material, resolvable_by_client}`).
2. **Module 4 is a two-field subset** `{is_load_bearing, risk_level}` — exactly and only what `computeVerdict` reads. G005's shape also has `contradiction_type`, `assertion_a/b`, `interpretation`. The coupling to the verdict engine is correctly minimal; the *stored/rendered* contract is missing the reasoning fields. Piping m4c-1.0.0 records into today's `module_4` type would **silently drop their audit fields**.
3. **Module 5 hypotheses are `unknown[]`** — G005's `{label, interpretation, supporting_evidence, contradicting_evidence, likelihood}` shape is untyped, and the "commit to a leading hypothesis, max three" discipline is structurally unenforced.
4. **Module 7 is three free strings** (`doubt_level` unvalidated) — G005's calibration inputs (`claim_observable_gap`, `cost_of_being_wrong`) do not exist in the contract at all.
5. `runSynthesis` is a typed placeholder returning empty modules (`doubt_level: "minimal"`, headline `"stub"`); **Module 1 normalization already exists as DETERMINISTIC CODE outside the engine** (`normalizeEvidence` — it also feeds `evidence_hash`/memoization). G005 draws Module 1 inside the LLM engine. **Frozen state wins and is better**: normalization stays code (see B2).

**Verdict on A1:** the scaffold matches G005's nine-module *skeleton* (names, count, storage columns, admin sections) but the *contract* is an early thin draft. This gate must type Modules 1–8 for real — flagged here plainly, not spec'd around.

## A2 — The seam inventory (every path synthesis → verdict)

| # | Path | Fields read | Status today |
|---|---|---|---|
| 1 | `computeVerdict(signals, synthesis)` → `synthesis.module_4_contradictions`: `filter(is_load_bearing).length ≥ 2` → **floor VBP**; `some(risk_level === "critical")` → **LOCK do_not_rely** | `is_load_bearing` (bool), `risk_level` (raw LLM string) | **WIRED — live at all three verdict sites** (pipeline `stageVerdict`, admin `buildVerdictViewModel`, `rejudge-case.ts`). Both fields trusted uncertified. Dormant only because the stub emits `[]`. **This is the seam S-0 certifies.** |
| 2 | `applyDocumentationNoOverride(result, signals, synthesis)` → calls `computeVerdict` a SECOND time (research-only recompute) with the same synthesis object | same two fields, via path 1 | **WIRED** — same exposure, second entry point. S-0 must cover it (certify once, upstream of both). |
| 3 | `applyVerdictCeiling` | none — reads signals only | **NO PATH.** |
| 4 | G005: "the proportional doubt level feeds the verdict engine as an input" (Module 7) | — | **NONEXISTENT.** No consumer of `doubt_calibration` outside storage + the admin panel. ADR-G004's locked algorithm has no doubt input either. **G005 vs frozen state: FROZEN WINS.** Doubt is NOT a verdict input, and S-0 locks that it cannot become one without its own certification gate (see B1/SO-S0-4). |
| 5 | ADR-G004 Part 4 paper vetoes never built: `contradiction_count ≥ 2 → floor` sourced from **Track 5** records, and `b2b_archetype_flag === true → LOCK do_not_rely` | — | **NONEXISTENT in code — and superseded.** Frozen `computeVerdict` floors on *synthesis* load-bearing count (not Track 5's), has no b2b flag input; the b2b lock was rebuilt as the Track-3 weight-key veto `b2b_only_confirmed` (g003-1.1.0 pure veto), and Track 5 is non-voting by AT-B1. **Frozen state wins; G004-paper divergences surfaced here, not silently reconciled.** Also G004's "Track 5 may raise verdict severity" is superseded by the T5 freeze: Track 5 raises NOTHING — its records inform Module 4, which is the certified path. |
| 6 | Layer 5 `buildReport` (Module 9 + 8 pass-through) | snapshot, vendor_questions | Wired but **not a verdict input** — a CLIENT-SURFACE path; governed by B4, not S-0. |
| 7 | `getClientDecisionSnapshot` (column-scoped Module 9 + 8 read) | snapshot, vendor_questions | Wired to **nothing** (tests only). Client-surface path for the PDF/client gate; carries a known annotation: **no ownership gate** (sweep INFO item) — must gain one before ever being wired. |
| 8 | Synthesis memoization (`evidence_hash` + `ios_version`) | — | Not a field path, but the versioning seam that decides WHICH synthesis object flows. The hardcoded ios model string (tracker rider) must be fixed inside this gate (S-2). |

**Complete inventory: exactly two live code entries (1 and 2), both funneling through the same two module_4 fields; zero for module_7 and everything else.** S-0's surface is therefore: certify module_4 before both entries, and lock that no new field can join the flow uncertified.

## A3 — m4c-1.0.0 vs Module 4's input contract

Frozen m4c-1.0.0 (contracts.ts): `contradiction_type: string` · `assertion_a/b: {track_key, statement, evidence_ids[]}` · `interpretation: string` · `risk_level: "low"|"medium"|"high"` (**code-capped — can never be "critical"**) · `is_load_bearing: boolean` (**always false from Track 5 — Module 4's judgment, by ruling**).

Against G005's Module 4 sketch (`contradiction_type`, `assertion_a/b` as plain strings, `interpretation`, `risk_level`, `is_load_bearing`):
- **Structurally SATISFIED, and richer:** m4c's assertion sides are structured with track + evidence IDs where G005 has prose strings — a superset; Module 4 can consume records as-is (projection to prose is trivial, the reverse is not).
- **Gap 1 — vocabulary:** m4c uses `low|medium|high`; G005's examples use `moderate`/`critical`; frozen code tests the literal `"critical"`. There is no certified enum anywhere. S-0 defines it: `low | medium | high | critical`, code-validated, with the m4c cap preserved on merge — a Track-5-originated record can NEVER be escalated to `critical` by copy-through; only a synthesis-layer contradiction that itself passes the S-0 structural test can carry `critical`.
- **Gap 2 — the storage type:** `SynthesisOutput.module_4_contradictions` is the two-field subset (A1.2) — Module 4's OUTPUT contract must be the full m4c-shaped record (contract version bumped m4c-1.0.0 → m4c-1.1.0 per the OQ-B2 door: additive `origin` field, see B2/M4), while `computeVerdict` continues to read only the two certified fields. Frozen core untouched.
- **Gap 3 — `is_load_bearing`:** m4c defers it (always false). Module 4 is where the judgment finally happens — and because ≥2 load-bearing = a verdict floor, load-bearing certification is inside S-0's scope (structural conditions, not trust).

## A4 — G005's ten action items vs the frozen state (frozen wins; divergences surfaced)

| # | G005 action item | Status | Notes (divergence surfaced where present) |
|---|---|---|---|
| 1 | Track prompts → evidence + reasoning + unknowns + confidence | **SATISFIED** | The hardening arc delivered beyond it: analyst-native prompts, per-item certainty/confidence, unknowns, questions, firewall validation, consensus. |
| 2 | Build `synthesisEngine.ts` Modules 1–9 | **OPEN** — this gate | With one divergence: Module 1 is already deterministic CODE (`normalizeEvidence`, feeds evidence_hash). Frozen state wins — Module 1 stays code (B2). |
| 3 | Synthesis prompt (fixed method) | **OPEN** — this gate | |
| 4 | Wire as step after tracks, before verdict | **SATISFIED** | `stageSynthesis` in both orchestrators, per-attempt persisted (H1), evidence-hash memoized — stronger than asked. |
| 5 | Verdict stays in deterministic `verdictEngine.ts` | **SATISFIED & HARDENED** | Three-site agreement + ceiling + no-override + dispute/replay. **Except:** G005's "doubt feeds the verdict" clause was never honored and G004 has no such input — frozen wins; doubt is advisory (A2.4, SO-S0-4). |
| 6 | Store reasoning role-gated; expose only the Snapshot | **PARTIAL** | Storage + admin role-gate + column-scoped client read all exist. But the client read is UNWIRED and lacks an ownership gate; and today the client sees NO synthesis at all — F2's allowlist (which postdates G005's visibility table) makes everything private by default. **Frozen state wins: G005's visibility table is re-based on the F2 allowlist** (B4); Module 9 ships only at the client-surface/PDF gate, deliberately. |
| 7 | Override capability (add-evidence / redact) | **OPEN — NOT THIS GATE** | Agency-mode machinery; its own gate. The B2C approve/rerun/override-verdict flow exists (admin review route). |
| 8 | Log overrides to `founder_decisions`/audit_log | **PARTIAL** | Verdict overrides + decisions audit-log today. `founder_decisions` table usage: **not confirmed** — verify at the agency-mode gate. |
| 9 | Cascade into Tech Arch v1.4 | **OPEN — founder docs task** | Planning set (`Docs\`) is founder-maintained; out of engineering scope. |
| 10 | Determinism: same evidence pattern → same verdict | **PARTIAL — machinery exceeds G005, ATs are this gate's** | Memoization gives same-hash → same synthesis BY CONSTRUCTION; F4 made the zero-API proof leg trustworthy; the synthesis-live determinism ATs are B6's. |

**Also surfaced, not reconciled:** G005's `SYNTHESIS_CONFIG` and the docs' tier table disagree with each other AND with the frozen plan registry (whose `plan_gates` are the only truth in force; G005's dimension names `brand_risk_basic` / `brand_risk_plus_keepa` / `sourcing_logic_deep` do not exist as track_keys). Handled in B3 — mechanism spec'd, mapping **UNRULED, EMPTY**.

---

# PART B — THE SPEC

## B1 · S-0 — THE SYNTHESIS→VERDICT FIREWALL MICRO-GATE (first, separate, frozen before Module 1 exists)

**Nature:** the entry condition ruled at the Track-3 gate, done the H7/registry way — as enforced code, not a memo. S-0 is spec'd → founder-signed → TDD-built → founder-AT'd → **FROZEN as its own commit** before any module code is written.

**The one new function:** `certifySynthesisForVerdict(raw: SynthesisOutput): CertifiedSynthesis` — a pure firewall applied **upstream of every entry into computeVerdict** (pipeline `stageVerdict`, admin viewmodel, `rejudge-case.ts`, and *inside the composition before* `applyDocumentationNoOverride`'s recompute — one shared function at every site, the ceiling pattern, or it doesn't ship). Frozen `computeVerdict` stays byte-identical; certification happens before its argument is formed.

**What certification does (all code, no trust):**
1. **Enum law:** `risk_level ∈ {low, medium, high, critical}` — anything else is clamped DOWN to `low` and recorded in a certification audit (never silently accepted, never guessed upward).
2. **Critical is EARNED structurally, never asserted:** a record may carry `critical` into the verdict path only if code verifies: both assertion sides carry `evidence_ids` that resolve to real Module-1 items, from ≥2 distinct tracks, at least one side certainty `verified`. Fails the check → clamped to `high` + certification audit. (Proposal — founder rules the exact structural conditions: SO-S0-2.)
3. **Load-bearing is counted, capped, and structurally gated the same way** (a floor fires at ≥2 — so `is_load_bearing: true` requires resolving evidence_ids; else coerced false + audited).
4. **m4c origin cap preserved:** records merged from Track 5 (`origin: "track5_m4c"`) keep their ceiling — copy-through can never mint `critical` or `is_load_bearing: true` from a Track-5 record (AT-B1's never-votes property, extended through the merge).
5. **Shape coercion:** non-array → `[]`; non-boolean load_bearing → false; every coercion audited.

**Structural locks (the registry-lock pattern — a failing test, not discipline):**
- **The poisoned-synthesis property test:** feed a synthesis object with adversarial content in EVERY field (critical-lookalike strings in module_7, module_9, module_5, unknown extra fields, prototype junk) — the verdict must be **byte-identical** to the same signals with an empty synthesis, except through certified module_4 records. This is the "no uncertified path" proof over the whole A2 inventory in one property.
- **Composition locks at all entry sites** (certify-then-compute asserted per site, like the ceiling/no-override site tests) + a source-scan lock that `computeVerdict` is called nowhere without certification.
- **Doubt lock (SO-S0-4):** module_7 is structurally advisory — the poisoned test plants a maximal doubt object and asserts zero verdict effect. Any future "doubt feeds the verdict" is its own founder-gated spec with its own certification, never a rider.

**S-0 ATs (founder-run):** AT-S0-1 poisoned-synthesis byte-identity (unit, the conscience) · AT-S0-2 rejudge on existing delivered cases reproduces stored verdicts (certification of stub/empty synthesis is a proven no-op — zero-API) · AT-S0-3 the structural-critical rules two-sided (an earning record passes; an asserted-only `critical` clamps + audits) · AT-S0-4 all-sites composition green · AT-S0-5 full suite + versions (VALIDATION bump justified here — certification IS firewall config; pins updated same commit).

## B2 · MODULES 1–9 — CONTRACTS (input → fixed reasoning move → output; all scenario-independent)

Orchestration: Module 1 is deterministic code; Modules 2–9 run as staged LLM calls (B7 — **re-ratified at FOUR calls with A3's refuter pass, founder 2026-07-16**) with per-call schemas (H7 structured-outputs pattern, fail-open + tolerant parser), each stage certified/validated in code before the next consumes it. **R2 (founder-homed to S-1, 2026-07-16): every stage's `schema_fallback` flag is PERSISTED to the synthesis record — a silent tolerant-parsing fallback is invisible in prod, and A5's backtest would see variance it cannot attribute.** Every output is per-attempt persisted (existing `case_synthesis` columns — **no migration expected**; the columns exist since the initial schema). **A1 build constraint (the Q3 answer, 2026-07-16): `computeEvidenceHash`'s input projection stays BYTE-IDENTICAL — the widened M1 record (rejections-with-gate, unknowns, advisory metadata) rides BESIDE the hash, never inside it. The hash remains the accepted-evidence anchor; nothing stored is invalidated.**

- **M1 — Evidence Normalization + Frozen-Record Assembly (CODE, exists + the A1 widening).** In: all `TrackOutput`s **+ the full frozen record (A1 APPROVED): the firewall's REJECTION RECORDS (every claim code refused + WHICH GATE refused it; corroboration-rejected items tagged `asserted_but_unverifiable`), unknowns, advisory metadata, consensus records, diversity records, the identity audit.** Move: flatten accepted evidence to `NormalizedEvidenceItem[]` (statement/certainty/source_type/url/claimant/claimant_benefits/supports + source_track) + `evidence_hash` — the hash projection stays BYTE-IDENTICAL (Q3), the widened fields ride BESIDE it, never inside it — and assemble the widened record, deterministic code end-to-end. Out: **the WIDENED M1 RECORD** = accepted evidence (typed as today) + `rejected_with_gate[]` + `unknowns[]` + advisory metadata + identity audit + consensus/diversity records. **M1's `claimant`/`claimant_benefits` are per-track PROVENANCE CLASS (code literals at the construction sites), never per-claim truth — per-claim attribution is M2's job, under DISTINCT names by the naming law (see M2).** The claim-observable gap arrives MEASURED by the firewall, not inferred by a model — the proprietary input representation (ADDENDUM-2 Move 2); it exists only downstream of our firewall. *Divergence from G005 (LLM-internal Module 1) surfaced at A4.2 — code wins: it is the determinism anchor.*
- **M2 — Claim Attribution.** In: **the WIDENED M1 record (A1) — accepted items AND rejected-with-gate items.** Move: for each claim-bearing item — who said it, who benefits, corroboration status. **A rejected-with-gate item arrives with its corroboration outcome ALREADY MEASURED by the firewall — M2 never re-infers a corroboration decision the firewall already made; the stored record wins over any LLM re-assessment.** **✅ RULED — APPROVED (founder, 2026-07-17; this law arrived attached to the A1 threading, unruled — caught and named). The SCOPING is what makes it correct, on the record: it locks the DANGEROUS direction (the LLM writing `independent` on a firewall-rejected claim — resurrecting a claim code the gate already killed) and leaves the CONSERVATIVE direction alone (the LLM writing `none_found` on a corroborated claim merely locks weight to `low_until_corroborated` — fails toward caution). Enforced by CODE from the stored tag, not by prompt: "LLM proposes, code decides" at module level, following from A1's thesis rather than extending it. Process note on record: laws that arrive attached to plumbing still get named as laws.** Out: `{evidence_id, claim, claim_attributed_to, attributed_party_benefits, corroboration: "independent"|"cross_source"|"none_found", weight: "standalone"|"low_until_corroborated"}[]`. **NAMING LAW (founder, 2026-07-17 — the `elevated`-collision lesson applied BEFORE the field ships): M2's per-claim attribution fields take DISTINCT names from M1's `claimant`/`claimant_benefits`. M1's pair is per-track PROVENANCE CLASS — code literals at the construction sites, the field the OQ-S5 cap keys on — never per-claim truth; M2's pair is per-claim ATTRIBUTION, LLM-derived. The rename lands on the UNBUILT side (M2's output costs nothing; M1's fields live in stored records and feed `evidence_hash` — the frozen-core-byte-identical logic). BOTH fields renamed, not just one: the benefits boolean carries the identical collision and is the half the M2 code lock keys on — half a rename would leave the load-bearing half ambiguous. Names chosen and why: `claim_attributed_to` (it is an attribution — a judgment about the claim, not a provenance class) · `attributed_party_benefits` (self-describing: does the party the claim is attributed to benefit from it being believed?).** Law: a self-serving uncorroborated claim can never be weighted as independent evidence (schema-enforced pairing: `attributed_party_benefits && corroboration==="none_found"` ⇒ weight is locked by CODE to `low_until_corroborated`, whatever the LLM wrote; a firewall-rejected item is locked the same way by CODE from its stored tag).
- **M3 — Assertion Engine.** In: M1 (widened) + M2. Move: testable assertions with status. Out: `{assertion_id, assertion, brand, status: "supported"|"refuted"|"unresolved", supporting_evidence[], contradicting_evidence[], confidence: high|medium|low}[]` — **`brand` is A8 APPROVED: every assertion carries a brand scope field, like track evidence already does — prevents cross-brand context bleed on multi-brand cases (the Amendment-2/subject-discipline lesson at synthesis altitude). A8 partitions; it does NOT aggregate — how multi-brand findings roll into ONE M9 narrative is OQ-S4's ruling, not this field's.** Code validation: every referenced evidence_id must resolve to M1 (dangling → item dropped + audited; the auditability property is structural).
- **M4 — Contradiction Engine.** In: M3 assertions + **the WIDENED M1 record (added 2026-07-17, SO-S1-1 Condition 1 — M4's inputs must be able to enforce M4's own document-diversity law, and the provenance lives in the M1 record)** + Track 5's stored m4c records (merged with `origin: "track5_m4c"`; synthesis-born records get `origin: "synthesis"`). Move: cross-track conflicts; the gap between claimed and observable is the finding. Out: **m4c-1.1.0** = m4c-1.0.0 + `origin` (additive bump through the OQ-B2 door; Track 5's writer stays on 1.0.0 — reader accepts both). Verdict engine reads only the two certified fields, post-S-0.
  **MODULE LAW — DOCUMENT-DIVERSITY SEMANTICS (routed from the Track 4 freeze, deferral iii; founder-homed here 2026-07-16):** same-transaction and post-shipment documents are NOT independent corroboration; the diversity-lift is dormant-by-design (real clients arrive with a single pre-payment document). M4's contradiction reasoning must never treat two artifacts of the same transaction as two independent voices — an agreement between them is consistency, not corroboration, and a conflict between them is ONE contradiction, not two.
  **PROVENANCE, STATED FROM SOURCE (2026-07-17, per Condition 1's stop rule):** what the frozen record carries — every evidence item's `source_url`; for document items this is the document's STORAGE PATH pseudo-URL (`documentPack.ts`: "distinct documents = distinct sources", deliberately, for dedupe + diversity), threaded onto each item at `track4.ts` (`source_url: src?.url`), so **SAME-DOCUMENT identity is deterministic**; document items also carry `source_type: "vendor_self_assertion"` + `claimant: "vendor"` + `claimant_benefits: true` + `source_track: "documentation_review"`, so **"vendor-supplied artifact" is a stored, code-checkable CLASS**. What the frozen record does NOT carry: **a TRANSACTION id grouping two DISTINCT documents** (invoice + packing list of one deal) — `uploaded_files` has no such column and no pack field encodes it. THEREFORE the law is code-enforceable at exactly two granularities: (1) same `source_url` = one voice — deterministic (the live precedent: AWI-2607-031's multi-block invoice reconciled as ONE document); (2) all documentation_review items are ONE vendor-supplied provenance class and can never corroborate each other into "independent" — code lock from stored fields, conservative (no cross-document corroboration lift at all; consistent with dormant-by-design). **THE RESIDUAL — recognizing that two DIFFERENT documents belong to the SAME transaction (for the conflict-is-ONE-contradiction half) — has NO stored field to bind: it is a judgment over document content and can only be prompt law, not code. ⚠ OQ-S5, FLAGGED NOT SILENTLY DECIDED** *(→ RULED same day — next paragraph)*.
  **MODULE-LAW AMENDMENT — OQ-S5 ✅ RULED, NEITHER FLAGGED OPTION (founder, 2026-07-17): THE CONFLICT-MERGE HALF IS CODE-ENFORCED AT THE VENDOR-DOCUMENT-CLASS GRANULARITY.** Both flagged options left the conflict-merge half unguarded by code on a path that REACHES THE VERDICT: `is_load_bearing ≥2` floors to verify_before_purchase, so one transaction's paperwork producing TWO load-bearing contradictions instead of ONE fires the floor on a case that did not earn it — and prompt-only enforcement on a verdict path is the Keepa class, the class this system's own history rejects. **RULED PRINCIPLE — AS CORRECTED BY THE FOUNDER (2026-07-17; his own first phrasing "the vendor is ONE CLAIMANT; one claimant is one voice" was TOO BROAD — taken literally it sweeps EVERY vendor claim anywhere (paperwork, website, marketing) into one capped voice, which is neither what the cap does nor what was meant; corrected on the record because a spec carrying a principle broader than its code is how someone later "fixes" the cap to match the words and swallows real contradictions): THE CAP'S SUBJECT IS ARTIFACTS OF ONE ACT OF CLAIMING — the vendor's SUBMITTED PAPERWORK. An invoice and a packing list for one deal are one act. The vendor's WEBSITE is a SEPARATE act, and a vendor-website-vs-observable contradiction is a genuine cross-source finding that MUST still floor. Contradictions sourced from the submitted-paperwork class contribute AT MOST ONE load-bearing record.** WHY IT WORKS WITHOUT A TRANSACTION FIELD (reasoning on record): you do not need to know WHICH transaction two documents share — they are artifacts of the same act of claiming by the same party; transaction identity was always a proxy for that, and the submitted-paperwork class IS stored (`claimant: "vendor"` identifies exactly that class — which is why the mechanism is right). The missing field was never the thing the law needed. **CONSERVATIVE BY DESIGN, the correct direction:** it under-counts genuine multi-transaction conflicts rather than manufacturing concern from one artifact — "documents raise concern, never manufacture comfort" does not license manufacturing concern either. The prompt law rides along as narrative discipline; it stops being the guard.
  **MECHANISM (S-1 build; frozen S-0 UNTOUCHED — the cap lives in M4's per-stage CODE validation, before certification consumes the record set, never inside frozen `certifySynthesisForVerdict`):** a contradiction record is DOCUMENT-SOURCED iff BOTH assertion sides' evidence_ids resolve to M1 items with `claimant: "vendor"`. **Ruling precondition VERIFIED FROM SOURCE (2026-07-17): claimant is a CODE LITERAL at every track's single evidence-construction site — track1 `"independent_registry"` (track1.ts:146), track2/track3 `"third_party"` (track2.ts:159 / track3.ts:155), track4 `"vendor"` unconditionally on every document item (track4.ts:162); Track 5 emits no evidence items (frozen AT-B1). Never LLM-written, never conditional — the ruling holds, and on the current engine `claimant:"vendor"` ⟺ documentation_review-sourced, so the cap cannot touch a web-sourced record. **THE EQUIVALENCE IS LOCKED, NOT CLAIMED (founder, 2026-07-17): as stated it is an invariant NOBODY ENFORCES — the cap keys on the literal, and a future track reading vendor-submitted data would join the capped class silently. S-1 builds a derived source-scan lock (the H7/registry pattern, derived not remembered): no evidence-construction site outside `track4.ts` writes `claimant: "vendor"` — a new site fails the lock BY NAME. Built with the cap, same commit.** Among document-sourced records, CODE counts at most one toward the ≥2 floor: the first stays as judged; every further document-sourced `is_load_bearing: true` is coerced false + audited (the S-0 audit pattern — visible, never silent). `risk_level` untouched (severity is not double-counting). Note: the `critical` lock is ALREADY structurally immune to the doc-vs-doc shape — SO-S0-2 requires ≥2 DISTINCT TRACKS, and both sides of a doc-doc record are documentation_review; only the load-bearing floor (deliberately given no track-diversity bar at SO-S0-2) needed this cap.
  **TWO-SIDED BY OBLIGATION (spec'd now, test-locked at S-1):** the cap binds ONLY document-vs-document contradictions — both sides vendor-document class, one claimant arguing with itself. A WEB-vs-DOCUMENT contradiction is UNAFFECTED and must stay unaffected: one side is an independent voice, so independence of voice exists and the record counts normally — that shape (documentation comfort vs web risk) is exactly the no-override scenario Track 5 records for Module 4, and a cap that silently swallowed it would be the inverse defect. S-1 ATs: (i) two doc-doc load-bearing records → floor count 1, second coerced + audited; (ii) one doc-doc + one web-doc load-bearing → floor count 2, floor fires; (iii) a genuine cross-source contradiction is never swallowed; (iv) the claimant-literal source-scan lock is green and RED-provable — poisoning a second construction site with `claimant: "vendor"` fails the lock by name.
  **RESIDUAL NOTED, STAYS OPEN AS A NOTE (not closed):** the transaction field remains absent. If a future case shows claimant-granularity is too coarse, that is a product change with its own describe-and-stop — never a quiet loosening of this cap.
- **M5 — Hypothesis Engine.** In: M3 + M4. Move: competing interpretations of the verdict-critical question; **commit**. Out: `{hypotheses: [{label, interpretation, supporting_evidence[], contradicting_evidence[], likelihood: "leading"|"alternative"}] (1–3), what_would_change_the_leader}`. CODE enforces exactly one `leading` and length ≤3 (truncate + audit). Never a verdict input.
- **M6 — Risk Gap Detection.** In: M1–M5 + the unassessed-dimensions list (from the tier seam, B3) + **the `dimension_run_record` (ruled 2026-07-17 — see M9's block for the shape). WHICH CONCEPT IS WHICH, stated so nobody reading cold assumes complements: B3's list = PLAN-EXCLUDED ONLY — the concept B3's law governs; `dimension_run_record` = the per-dimension EXECUTION record preserving cause. They are NOT complements: an attempted-and-failed dimension is in neither B3's list nor the assessed set. THE CAUSE SELECTS THE LAW: plan-excluded → B3's limitation language ("your plan doesn't include X"); attempted-and-failed → H2's law (our infra failure is never a claim about the supplier — "we attempted X and it did not complete"); nothing-to-review → OQ-A3's ruled absence sentence. Three different honest sentences — the field exists so M6 and M9 can apply the right one.** Move: what is unknown and does it matter. Out: `{gap_id, unknown, why_it_matters, is_material, resolvable_by_client}[]`. Law: a plan-excluded dimension may appear ONLY as a stated limitation, never as a material gap counted against the case (code filter — the N2 lesson at synthesis altitude).
  **MODULE LAW — FINANCIAL SCOPE (routed from the Track 4 freeze, deferral ii; founder-homed here 2026-07-16; applies to M6 AND M8):** the engine assesses vendor-legitimacy anomalies and missing/expected paperwork ONLY — never transaction economics (freight cost, price reasonableness), never a nudge toward or away from the buy decision. The Morendelli preview's 11,300-EUR air-freight flag is the named pattern to remove: M6 must not inherit that instinct as a material gap, and M8 must not convert it into a vendor question. Whether the deal is a good deal is the client's decision — the reason they came to us.
- **M7 — Proportional Doubt Calibration (the heart of the IP).** In: **calibration inputs, not scenario rules** — (a) `claim_observable_gap`: **CODE-derived from the A1 record's REJECTION RECORDS + M3 statuses, per the doubt-matrix skeleton's definitions: R = load-relevant claims that DIED AT THE CORROBORATION GATE or sit `unresolved` (the firewall's rejection records — MEASURED, not inferred), S = load-relevant assertions `supported` with certainty `verified`. The gap is never inferred from LLM-written corroboration/status fields alone — the firewall's stored, deterministic records are the primary source; that substitution is A1's entire thesis. **[⛔ GAP DERIVATION RULED DEAD 2026-07-17 (A1 split ruling — widening stands, mechanism dead: corroboration deaths are empty on this corpus and wrong-polarity on any corpus; founder principle: R measures the vendor's OWN unobserved story, never allegations against them). Text preserved, not deleted, per the correction discipline.]** **✅ REPLACEMENT RULED — THE PRE-COMMITTED FALLBACK FIRES (founder-confirmed 2026-07-17, after FOUR candidates enumerated and killed on the record — which is itself the finding): `claim_observable_gap` v1 is LLM-DERIVED — M3 `unresolved` statuses + the stored unknowns — TRUTHFULLY LABELED: the gap axis is LLM-derived; the cost axis is deterministic (OQ-S1 (a)); the LLM therefore moves ONE of the two matrix inputs; blast radius bounded to narrative tone by the S-0 lock, which is unaffected by any of this. WHY THE MOAT SURVIVES (founder's reasoning, on the record): the LLM NEVER CHOOSES doubt_level — it reports counts; THE FOUNDER'S MATRIX decides what those counts MEAN, and the conversion stays founder-authored, versioned, deterministic. An LLM writing more `unresolved` moves an input; it cannot declare a case broad-doubt. The paranoia machine still dies at the conversion step — that was always A2's real mechanism, not input purity. Fourth-candidate close-out: source_type CLEARED authorship (code everywhere, parser-proof) but the axis as specified was never deterministic (observed side gated on LLM-written `certainty`), and 38/65 zeros with ~3.4× same-vendor variance is INSTABILITY, worse than sparsity — an axis that moves that much on one vendor is not measuring the vendor. Re-derivation path = the G4 entry condition (skeleton). NO FIFTH HUNT.** **HONESTY LABEL (SO-S1-1 Condition 2, founder 2026-07-17 — the claim must match the artifact):** the gap axis is PRIMARILY deterministic (the firewall's rejection records) with ONE LLM-written input — M3 `unresolved` statuses — so the LLM retains an indirect, one-module-upstream lever on doubt_level by writing more `unresolved`. The design does not change; the blend is right (`unresolved` captures gaps the firewall never gated — excluding it would blind R to them). A2's "the LLM writes WHERE, never HOW MUCH" is therefore APPROXIMATELY true, not structurally true: doubt_level is code-computed from inputs that are primarily but not wholly deterministic, and **the property that makes this safe is not input purity but the S-0 lock** — doubt is advisory, structurally locked out of the verdict; the lever moves narrative tone and nothing else. *[SUPERSEDED 2026-07-17 by the fallback ruling ABOVE — the gap axis is now WHOLLY LLM-derived; the safety property carries over unchanged. (Direction corrected 2026-07-17 — the note originally said "below".)]***; (b) `cost_of_being_wrong`: **OQ-S1 ✅ RULED (a) (founder, 2026-07-17): OBSERVABLE ENFORCEMENT STAKES ONLY** — Track 3 enforcement-posture signal + validated veto-grade keys present + breadth of brands at issue. **NO intake field.** Rationale on record: order value / account exposure are not collected, and adding an intake field is a product decision with a client-surface cost — it belongs at the client-surface gate where intake is already being touched, not smuggled into the engine build; and the axis stays HONEST — everything feeding it is something the system OBSERVED, never something the client ASSERTED (a self-reported order value would put an unverified claim inside the calibration of the module whose entire job is measuring unverified claims). Move: `doubt_level` computed by the FOUNDER-AUTHORED matrix (A2; `DOUBT_MATRIX_VERSION d7-1.0.0` versioned config; cost axis `low|significant|severe`); the LLM writes only `doubt_focus` + `rationale` — WHERE the doubt lands, never HOW MUCH. **✅ THE MATRIX IS FILLED AND CONFIRMED (founder, 2026-07-17 — every cell in the skeleton file; thresholds blank for G4; Axis-2 level meanings PROPOSED there for founder confirmation). THE FOUNDER'S LAW, the governing law of the matrix (binds M7's calibration AND M9's rendering): "DOUBT IS CALIBRATED TRUST IN A STATED CONCLUSION. CONFUSION IS THE ABSENCE OF ONE." Every level — including `broad` — states the verdict sentence identically and commits to a leading reading; the test for every cell is CAN THE CLIENT ACT ON IT. Governing principles recorded with the cells: gap sets the level, cost only modulates within it (high stakes never manufacture doubt — the paranoia machine dying at the conversion step); doubt framing has a cost — it buries the answer, so it must earn its place. G4 flag on record, not acted on: `severe` never differs from `significant` anywhere in v1 — a G4 tuning question a blank grid would never have surfaced.** Out: `{doubt_level: "minimal"|"targeted"|"elevated"|"broad", doubt_focus, gap_inputs, cost_inputs, rationale}` — the inputs ride the record so calibration is auditable and later tunable against the outcome corpus. **Advisory only — shapes Module 9's tone; structurally locked out of the verdict (S-0).** **THE DERIVATION RULE binds `rationale` (SO-S1-4 condition, founder 2026-07-17): derived from rejection records, it may state WHAT IS UNVERIFIED, never WHAT REFUSED IT OR WHY — no gate names, no thresholds, no corroboration counts, no firewall vocabulary (prompt law + scanner class; full statement at B4-EXT).** **THE MATRIX IS TIER-INDEPENDENT (founder, 2026-07-17 — recorded; a DIFFERENT claim from A2's "scenario-independent", which never covered it): ONE doubt matrix, the SAME matrix at every tier. The tier changes what evidence EXISTS; the matrix converts evidence-state → doubt IDENTICALLY whether a case ran two tracks or six, Keepa or no Keepa. A cheaper tier does not get worse judgment — it gets the SAME judgment, honestly stated, with the doubt its thinner evidence earns. Without this recorded, someone at the tier-mapping ruling reasonably proposes a tighter matrix for a premium tier — the answer is NO: the method is the moat and the method has no tiers. Consistent with B3's law (plan-excluded dimensions are LIMITATIONS, never material gaps): the gap axis measures what is unverified WITHIN what was assessed; a client is never charged doubt for the tier he bought.**
- **M8 — Vendor Question Generator.** In: M6 gaps (material AND resolvable_by_client) + tracks' stored `questions_to_ask`. Move: convert to specific pre-commitment questions; dedupe. Out: `string[]` (client-facing → HARD-scanned at delivery). Material-but-unresolvable gaps route to M9 as stated limitations instead. **The FINANCIAL-SCOPE law (M6, above) binds M8 identically — no transaction-economics questions, ever.**
- **M9 — Decision Snapshot Builder.** In: M5 leading + M7 doubt + M6 material items + M8. Move: the only client-facing assembly — plain language, template-disciplined, banned-language-native (the prevent-not-rewrite law), states non-assessed dimensions as limitations. Out: existing `DecisionSnapshot` shape (contract keeps `what_to_verify`; the UI's Module-8 sourcing stays the law — surfaced at A1, founder may retire the dead field at the client-surface gate).
  **MODULE LAW — CROSS-TRACK STANDPOINT (routed from the Track 4 freeze, deferral i; founder-homed here 2026-07-16):** the tracker names synthesis explicitly — "Track 2 AND SYNTHESIS can make the same 'treat the client as a stranger / interrogate the report's own premise' error," and M9 is the module that writes to the client, so M9 is where it lands. The submitter-standpoint law (the buyer on the client's own documents is normally the client; never question who the submitter is) and the scope/label-neutrality law (pre-payment paperwork labels carry no weight; deal completion is the client's decision, never a gap) apply UNIFORMLY to M9's narrative — the same f153fd2 laws, at synthesis altitude, prompt-law + test-locked like Track 4's.
  **MODULE LAW — THE DERIVATION RULE (SO-S1-4 condition, founder 2026-07-17; binds M9's narrative exactly as it binds M7's rationale):** narrative derived from rejection records may state WHAT IS UNVERIFIED, never WHAT REFUSED IT OR WHY — no gate names, no thresholds, no corroboration counts ("two independent sources"), no firewall vocabulary. Prompt law + a new scanner class at delivery; the leak example and full statement live at B4-EXT.
  **OQ-S4 ✅ RULED (i) (founder, 2026-07-17): ONE CASE VERDICT — the FROZEN behavior, untouched (per-brand vetoes pool into one track signal, worst-brand-governs by construction; option (iii), touching verdict math, stays rejected). M9 NAMES THE DRIVING BRAND and CARRIES THE PER-BRAND STATUS.** Reasoning on record: the single_99(a) ruling applied to the multi-brand case — attribute the flag to the BRAND, never the supplier ("your supplier is solid; the brand carries resale risk"); a seller buying five brands from one vendor needs to know WHICH one is hot so they can buy the other four — "verify before purchase" alone is not actionable. **THE CONTRACT (shape, not rendering):** (1) **driving-brand naming is PROMPT LAW on the EXISTING narrative fields** — when brand-scoped findings drive the verdict band, headline/the_real_risk must name that brand; no shape change; HARD-scanned like every M9 string. (2) **The per-brand status is STRUCTURED DATA and cannot ride prose — it requires an additive field. HOME ✅ RULED (founder, 2026-07-17): a SIBLING FIELD ON `SynthesisOutput` beside module_9 — NOT DecisionSnapshot.** Deciding argument on record, mechanical: M9 is an LLM call and its Out is the model's response schema; brand_status is CODE-DERIVED — a code-derived field in an LLM response schema either has the LLM writing it (contradicting the derivation) or gets code-merged post-call into the model's shape, putting TWO AUTHORSHIP CLASSES in one container (the claimant/`elevated` collision again; every scanner built for that container assumes prose). **S-0-safe CONFIRMED FROM SOURCE (not assumed): `certifySynthesisForVerdict` rebuilds from `emptyVerdictInput()` — an unknown sibling can never reach computeVerdict; the poisoned test's unknown-extra-fields coverage is exactly this. S-0 untouched.** Sibling name PROPOSED (founder rules): **`brand_evidence_status`** — names the fact class (where evidence landed), no verdict-adjacent word, deliberately no module_9 prefix (it is not the M9 LLM call's output). Client path: F2-allowlisted out by default; the client-surface gate projects it when it rules rendering (client-value #7); B4-EXT row added. **SEMANTICS ✅ RULED AS PRINCIPLE (founder, 2026-07-17): THREE STATES MINIMUM — `assessed_no_adverse` · `assessed_adverse` · `not_assessed` — and NOT-ASSESSED NEVER COLLAPSES INTO CLEARANCE** (it renders as a limitation, never clearance — B3's law at brand granularity; collapsing rebuilds AWI-2606-001 at brand granularity: "four brands clear" when one was never looked at). No new client vocabulary; the four verdicts stay case-level; brand_status is a fact about where evidence landed, never a per-brand verdict; no fourth state invented.
  **⚠ CORRECTED BY RULING (founder, 2026-07-17, on the (a)–(d) verification — his own three-state ruling was WRONG, original preserved above per the standing discipline): TWO STATES, NOT THREE.** The (c) finding is decisive: within a ran track, a brand with zero items is indistinguishable between assessed-clean, acquisition silence, and the model ignoring it — so the three-state ruling COLLIDED with its own never-render-as-clearance rule (if assessed-clean and not-assessed are indistinguishable and not-assessed cannot be clearance, NEITHER can be — they render identically; the third state was a distinction the record cannot support). **THE RULED SHAPE IS ASYMMETRIC — "absence of evidence is never a finding" (ADR-G004) at brand granularity: `adverse_findings_attributed` (positive evidence exists; LLM-attributed, but the item's own statement names the brand — text-corroborated, not a bare tag lookup) · `no_adverse_findings_attributed` (ABSENCE — NEVER a clearance; true whether the brand was clean, acquisition was silent, or the model skipped it; renders as a limitation).** OQ-S4's value survives whole: the buyer learns which brand is hot and that the others carry no attributed findings — without ever saying "clear" about something we may not have looked at.
  **PAYLOAD ✅ RULED (founder, 2026-07-17 — spec RETURNS for confirmation before S-1 writes it):** name `brand_evidence_status` ACCEPTED · **ROSTER LOCK UNCONDITIONAL** (tag ∉ `brands_submitted` ⇒ dropped + audited — closes the hallucinated-brand hole; the roster is code-side per (b), the tag is schema-validated-against-nothing per (a)) · **the deterministic half rides SEPARATELY as its own code-derived CASE-level field — ✅ RULED WITH THE CAUSE PRESERVED (founder, 2026-07-17: `dimensions_assessed` was the ANTONYM BY NAME of B3's unassessed-dimensions list and NOT its complement in fact — B3's list is PLAN-EXCLUDED only, while the causes also carry attempted-and-failed; collapsing them denies the client the difference between "your plan doesn't include documentation review" and "we attempted documentation review and it did not complete", and breaks M6's ability to apply B3's law to the thing B3's law is about). VERIFIED FROM SOURCE (2026-07-17): the causes SEPARATE CLEANLY, via TWO code mechanisms — a plan-excluded track writes NO row at all (the orchestrator omits it; `cases.track_N_status = 'skipped'`; derivable from the code plan registry + `plan_type`), while attempted-and-failed writes a row flagged `acquisition_failed` / `llm_failed` (pipeline.steps branches). ⚠ AND the stored causes EXCEED the ruled three states — flagged, not silently mapped: `nothing_to_review` (built, ran, zero uploads — client-caused absence, OQ-A3's own ruled sentence, a THIRD different client sentence) and `not_implemented` (not built — currently unreachable, all five tracks built) are also live flags. PROPOSED SHAPE (founder confirms — preserves the cause VERBATIM instead of forcing causes into a state enum): per dimension `{ dimension, state: assessed | not_assessed, cause: plan_excluded | acquisition_failed | llm_failed | nothing_to_review | not_implemented | null }` — state binary, cause from the stored flags / row-absence, all code. NAME PROPOSED: `dimension_run_record` — "run" names EXECUTION (what ran and why not), not assessment coverage, so it cannot read as the antonym or complement of B3's list; "record" matches the state+cause shape; no verdict-adjacent word. The which-concept-is-which cross-statement is recorded in M6's In.** · `driving: boolean` KEPT, **LABELED LLM-ATTRIBUTED** (honest label per (d): cannot move a verdict — case-level, vetoes pool at the frozen track layer before any tag is read — but CAN put the wrong brand name in the client's sentence; stated, not smoothed) · **option (β) DECLINED FOR THIS GATE, recorded as the future path** (EvidencePack is a FROZEN CONTRACT; its own gate; if (β) lands, attribution becomes deterministic and this ruling is re-derived — entry condition recorded beside the G4 re-examination path in the skeleton). (3) **Option (ii) RE-HOMED, not rejected:** how the surface RENDERS the per-brand status (split table / mini-snapshots / line per brand) is report design → the client-surface/PDF gate, recorded there alongside client-value #6 (group Questions-to-Ask by brand). **SCOPE NOTE (founder, so nobody re-litigates settled ground):** this ruling is about AGGREGATION, not DEPTH — brand-research depth is settled per the founder's record (web-search brand risk at every tier; Keepa seller-dynamics depth at Scale/Agency only — stated by the founder; the tier→track MAPPING remains UNRULED and empty per B3, unchanged by this). **CONSEQUENCE recorded:** with doubt PER-CASE, OQ-S1 (a)'s third Axis-2 input (breadth of brands at issue) is coherent — per-brand doubt would have made it a breadth of one.
  **DOUBT-LEVEL OUTPUT CONTRACT (authored on founder order 2026-07-17 — PROPOSED, build-thread-authored; founder confirms with the matrix fill. The omission was OURS: M7 said "shapes Module 9's tone" and M9 said "template-disciplined", and nobody stated what `broad` produces that `elevated` does not — the founder was being asked to calibrate a dial with no stated output.)** What each value CONCRETELY produces in the snapshot — SHAPE, not example prose. Invariant at every level: the verdict sentence is stated IDENTICALLY (doubt shapes the narrative around the verdict, never the verdict — S-0), and the derivation rule + banned-language law bind unchanged. **THE FOUNDER'S LAW BINDS THIS RENDERING (2026-07-17): "doubt is calibrated trust in a stated conclusion; confusion is the absence of one" — every level commits to a leading reading; `broad` is not a shrug, it is "here is my best account, and here is why you should not wire money against it yet"; the test for every cell is CAN THE CLIENT ACT ON IT — if `broad` ever produces a non-answer, the report failed.**
  - `minimal` — no doubt framing anywhere: headline and leading_interpretation state the reading unqualified; unknowns appear only in the stated-limitations slot; what_to_verify carries only tier-limitation items (may be empty); the_real_risk names the residual risk in one clause, no doubt language.
  - `targeted` — exactly ONE named qualification: the_real_risk MUST name doubt_focus's subject as the one open question; what_to_verify leads with the vendor questions that resolve THAT subject; headline and leading_interpretation stay unqualified.
  - `elevated` — the qualification moves INTO the reading: leading_interpretation is stated WITH its dependency (the reading rests on the doubt_focus subject holding); what_to_verify becomes the primary action block, framed as pre-commitment steps; the_real_risk states the consequence if the unverified claims fail; headline carries one conditional clause.
  - `broad` — the snapshot INVERTS: headline leads with what could NOT be verified rather than what was found; leading_interpretation is presented as the best available reading, explicitly not a confirmed account; what_to_verify is framed as a precondition to reliance, not an enhancement; the_real_risk states the claim-observable gap itself as the risk.
  **ENUM READING — RECOMMENDATION (reasoning on record; the enum is contract-fixed and does NOT change):** the enum is an **INTENSITY LADDER** (minimal < targeted < elevated < broad), not a flattened intensity×breadth pair. Breadth is not a free second dimension in this design: A2 gave WHERE to `doubt_focus` (LLM-written) and the level says HOW MUCH — and in the matrix's own construction intensity and breadth co-vary (a wider claim-observable gap is simultaneously more doubt and doubt about more of the story), so each rung's name reads off its most salient property. G005's pairs ("light, targeted" / "heavy, broad") are recovered as level × doubt_focus, not lost. No re-shaping is required.

**Analyst quartet rule (OQ-D lineage):** every narrative field in M2–M8 is ADMIN-ONLY until the client-surface/PDF gate — enforced structurally by B4, not by rendering choices. C3's standing requirement (quartet-class fields get full scanner scrutiny before ANY client surface) applies to M9 + M8 at delivery.

## B3 · TIER-GATING — A PARAMETERIZED SEAM; MAPPING **UNRULED — FOUNDER RULING PENDING**

G005's `SYNTHESIS_CONFIG` and the docs' tier table both descend from Business Plan v1 and **contradict each other**; neither matches the frozen plan registry. **Neither is copied. Nothing is invented. This spec contains no prices and no per-tier dimension list.**

**The mechanism (spec'd now):** synthesis consumes exactly the dimensions that RAN — derived at runtime from the attempt's stored rows, the same per-attempt truth every other layer uses; nothing tier-specific is hardcoded in the engine. A dimension with no row / `n_a` enters as **absent**: excluded from M2–M5 inputs, barred from M6 materiality (limitation only), stated in M9 as "not assessed in this report" in limitation language (never a finding, never a penalty — the H3/N2 law at synthesis altitude; the existing ceiling + weight redistribution already govern the verdict side and are untouched). The engine must function with the minimum dimension set the registry allows and never fabricate an excluded dimension.
**The parameter (empty):** `SYNTHESIS_TIER_MAP: —— UNRULED — FOUNDER RULING PENDING ——` · sole source once ruled = the plan registry's `plan_gates` (single source of truth; no second table). Any future tier redesign edits the registry, not the engine.

## B4 · RIDER 2 — FIELD-BY-FIELD CLIENT-STRIP PROOF (every field presumed a leak until proven)

**Baseline facts (verified in code):** the client findings path (`getCaseFindings`) selects only `case_track_results` columns and projects `compiled_findings_json` through the F2 **allowlist** — synthesis never enters it, and no synthesis field can ever ride it (future-fields-private-by-default is test-locked with a sentinel). The ONLY client-capable synthesis read is `getClientDecisionSnapshot` — column-scoped to `decision_snapshot, vendor_questions`, currently **wired to nothing**, and **missing an ownership gate** (known INFO item — a hard precondition below).

| Module / field group | Storage | Survives to a client payload? | Proof (existing or S-gate lock to build) |
|---|---|---|---|
| M1 normalized_evidence · M2 claim_attributions · M3 assertions · M4 contradictions · M5 hypotheses · M6 risk_gaps · M7 doubt_calibration (all fields incl. rationale/inputs) | `case_synthesis` columns | **NO — never** | (P1) column-scope lock: a test asserting `getClientDecisionSnapshot`'s select is exactly the two client columns — any widening fails; (P2) import lock: no client-surface data module reads `case_synthesis` except that one function (source-scan test, the F5-lock pattern); (P3) the F2 sentinel lock already proves the track-row path cannot smuggle any new field; (P4) RSC audit AT: DevTools sweep on a delivered case for module-content sentinels (the AT-B5/F2 method). |
| M4 records specifically (the double-home risk: same records also live in track_5's `compiled_findings_json.sourcing_logic`) | both tables | **NO** | F2 allowlist drops `sourcing_logic` (sentinel-locked, shipped); P1–P4 cover the synthesis home. |
| M8 vendor_questions | `case_synthesis` + report payload | **YES — by design, at delivery only** | Ships only through the snapshot fn after: (G1) ownership gate added (`client_id` check — precondition to ANY wiring); (G2) the delivery HARD-scan **extended to `case_synthesis` client columns** — today `review/route.ts` scans track rows + questions + identity note ONLY; **named gap, closed in this gate**; (G3) delivered-attempt pinning (already present). |
| M9 decision_snapshot (headline, leading_interpretation, the_real_risk, what_to_verify, what_to_monitor) | same | **YES — the product, at the client-surface/PDF gate** | Same G1–G3 + the C3 scanner-scrutiny requirement + sentinel-serialization AT on the wired page (plant reasoning-flavored sentinels in M1–M7 of a test case; assert zero survive the delivered page payload). |
| ios/version vector (model, versions, evidence_hash) | same | **NO** | P1/P2 (not in the client columns); admin QA view only. |
| Module fields inside admin viewmodel | in-memory | Admin only | Role-gated route (existing test pattern); never serialized to portal pages (P4 sweep). |

**Rule going forward (the structural answer to Rider 2):** any NEW synthesis field is client-invisible by construction — it would have to be added to the two-column select AND pass G2's scanner extension AND the sentinel AT to ever ship. Leak-by-default is inverted.

### B4-EXT · A1'S FIELD CLASSES (SO-S1-4 material — founder-ruled at the A1 sign-off, recorded 2026-07-17; due before S-1 builds)

B4's original table was written before A1 existed. A1 widens the M1 record with field classes it never contemplated — above all **REJECTION RECORDS: every claim code refused, and WHICH GATE refused it.** That is the firewall's gates written down, and per ADDENDUM-2 Move 5 it is the single most method-revealing artifact the system produces — twenty purchased reports would teach a competitor the FUNCTION, not just the format. **EVERY FIELD BELOW IS PRESUMED A LEAK UNTIL ITS ROW SAYS OTHERWISE. F2's allowlist is the FLOOR, not the proof.**

| A1 field class | Storage home(s) | Survives the client strip? | Proof (built at S-1 — PROVEN, not assumed) |
|---|---|---|---|
| **Rejection records** (claim code refused + refusing gate) | widened M1 record (`case_synthesis` normalized-evidence column, BESIDE the hash per Q3) | **NO — NEVER, at any gate.** Not even a client-surface-gate candidate: it is the method itself (Move 5). | P1 column-scope lock + P2 import lock + **P5-rej sentinel AT: plant a uniquely-tagged rejection record (fake gate name) in a test case's M1 record; assert zero bytes survive ANY delivered client payload — page RSC flight AND `getClientDecisionSnapshot` — the AT-B5/F2 DevTools method** + G2's scanner extension over the client columns it must never enter. |
| **Unknowns** (M1-widened) | widened M1 record | **NO pre-gate.** Client sees only M6→M9 LIMITATION LANGUAGE (HARD-scanned, G2) at the client-surface gate — never the raw records. | P1 + P2 + P4 sweep with an unknowns sentinel; raw records never join the two client columns; M9's limitation strings pass G2 + the C3 scanner-scrutiny bar. |
| **Advisory metadata** (b2b etc.) | DOUBLE-HOME: track rows' `compiled_findings_json` + widened M1 | **NO** | Track-row home: F2 allowlist sentinel (shipped) — the floor. Synthesis home: P1 + P2 + P4 with an advisory sentinel. |
| **Identity audits** (research_name/alias, resolver audits) | TRIPLE-HOME: `supplier_identity` (PG-1 projection, shipped) + track rows (F2, shipped) + widened M1 | **NO** | Existing homes: PG-1 projection (AT'd, two-sided DevTools proof) + F2 sentinel — the floor. New M1 home: P1 + P2 + P4 with an identity-audit sentinel. |
| **Consensus records** (`hard_fail_consensus`) | DOUBLE-HOME: track rows + widened M1 | **NO** | F2 sentinel (shipped) — the floor. Synthesis home: P1 + P2 + P4 with a consensus sentinel. |
| **Diversity records** (`source_diversity`) | DOUBLE-HOME: track rows + widened M1 | **NO** | F2 sentinel (shipped) — the floor. Synthesis home: P1 + P2 + P4 with a diversity sentinel. |
| **brand_evidence_status** (the OQ-S4 carrier — CODE-merged sibling on SynthesisOutput, NOT M9 LLM output; added 2026-07-17) | `case_synthesis`, sibling beside module_9 | **NO pre-gate.** The client-surface/PDF gate rules its rendering (client-value #7); until then admin-only like everything else. | P1 column-scope (not in the two client columns) + P2 import lock + P4 sweep with a brand-status sentinel + G2's scanner over it if/when it ships + S-0-safe by construction (certification rebuilds from scratch — source-verified 2026-07-17). |

**Execution note:** the P5 sentinel family is ONE parameterized AT — plant all six class sentinels in a single test case's M1 record and assert none survive any client payload (extends B4's M9 sentinel-serialization AT to A1's classes). Every future A1-class field addition must add its row here BEFORE it ships — the presumption-of-leak is the standing default, per the Rider-2 law.

**THE DERIVATION RULE (SO-S1-4 condition, founder 2026-07-17 — the SEMANTIC half of the proof; the table above is the FIELD half).** Field-level proofs catch COPYING; Rider 2's lesson is that the protected thing escapes by DERIVATION. P5-rej plants a fake gate name and asserts zero bytes survive — it does not catch this: *"The supplier's claim of authorized distribution could not be corroborated by two independent sources."* M7's `rationale` is DERIVED from rejection records and flows to M9. No rejection record shipped — THE CORROBORATION GATE'S THRESHOLD just did, in a field the table marks as legitimate client output. The firewall's gates, reconstructed from prose: Move 5's exact harm, past a byte-level sentinel. The table cannot catch it because the leak is SEMANTIC. **THE RULE: narrative derived from rejection records may state WHAT IS UNVERIFIED, never WHAT REFUSED IT OR WHY.** Enforced twice: (1) **prompt law on M7 and M9** (recorded in their B2 contracts); (2) **a NEW SCANNER CLASS** (the banned-language pattern): gate names, thresholds, corroboration counts, "two independent sources", firewall vocabulary — added to the delivery scan (G2) over every client-bound narrative field.

## B5 · THE G006 SEAM — MEMORY BETWEEN SYNTHESIS AND VERDICT (placeholder now, engine at G6)

G006 places Institutional Memory between Synthesis and Verdict; G005's diagram omits the slot. **The seam is reserved NOW so G6 never retrofits through the firewalled seam:** the S-0 certification boundary gains one reserved, versioned input — `memory_signal: null` in v1 — flowing THROUGH certification like everything else. Contract (reserved, unbuilt): memory may only ever contribute a **historical-risk advisory** that (a) passes its own future certification, (b) can never lock/floor a verdict alone (informs, never overrides — G006's own read-side determinism guard), (c) carries `corpus_version` (the `IosVersion` field already exists for exactly this). Wiring anything real into it is the G6 gate. S-0's poisoned test covers the slot from day one (a stuffed memory_signal must byte-change nothing in v1).

**G006 G3 write-side audit (satisfied / gap, per item):**
1. *Intelligence writes on every completed case* — **SATISFIED, stronger than asked:** H6's append-only `intelligence_events` + recomputable rollups supersede G006's mutable append/increment rows; F5 adds adopted-flow-only rollups. Divergence surfaced: G006's mutable-profile design is superseded by ledger+rollup — frozen wins.
2. *normalizeName solid* — **GAP, named:** the tracker carries "normalizeName too weak as universal key (merges different companies; empty-string CJK)". It is the corpus join key G6 will match on. Not this gate's scope — but it must be fixed BEFORE the G6 read-side, and is hereby pinned as a G6 entry condition.
3. *30/90-day outcome collection* — **SATISFIED with a ruled divergence:** H6's outcome-checkpoints cron + Outcome panel exist; H6 OQ-3 ruled ADMIN-digest-only where G006 sketched client emails. Frozen ruling wins; client-facing collection is a product decision for later.
4. *No case complete until writes fire* — **DIVERGENCE, ruled:** H2 OQ-2's loud-but-non-fatal contract (degraded:true + audit, case still finalizes) supersedes G006's hard block. Frozen wins; surfaced, not reconciled.
Plus the standing forward-flag at the caching/ADR-008 gate: rollups-follow-adoption (F5) — G6 inherits it.

## B6 · DETERMINISM PLAN (Action Item 10, verified — not asserted)

Two distinct properties, verified separately:
1. **Same stored record → same verdict (zero-API, absolute):** unchanged foundation — rejudge re-derives via the shared compositions (F4's `rederiveStoredSignal` + certify→compute→no-override→ceiling). S-0 extends rejudge with certification; AT-S0-2 proves no-op on the existing corpus. This property admits ZERO drift, ever.
2. **Same frozen evidence → same verdict across fresh syntheses (the G005 DD property):** ~~by construction where the memo hits~~ **RETRACTED (Q4 ruled (b), founder 2026-07-16): synthesis memoization is DISABLED at S-1 — no memo, no by-construction. The DD property rests ENTIRELY on AT-SYN-D + the certified projection.** **AT-SYN-D** — replay a delivered synthesis-bearing attempt TWICE (fresh reasoning both times — no memo special-case exists to disable) → narratives may differ; the **certified verdict-relevant projection** (certified module_4 set, signals, final verdict, confidence) must be identical. A would-be divergence is returned as near-miss data — never tuned away (standing law). Memoization returns only at the caching/ADR-008 gate, keyed on the FULL synthesis input per that gate's recorded entry condition.
**Canary ride-along:** the monthly panel adds one synthesis-bearing delivered attempt. The zero-API leg re-derives through certification (judgment drift, zero tolerance); the replay leg watches extraction+reasoning variance with the double-replay noise floor; the seeded envelope (band-boundary re-inference swung a track ~5 pts / one band with zero drift) prices the NARRATIVE noise floor — but certified-field drift has **no floor**: any above-zero certified drift on the zero-API leg is stop-the-line. `synthesis_version` joins the version pins (rerun/dispute preflights) and the ios model string is derived from MODEL_CONFIG in this gate (S-2, existing rider).

## B7 · ONE CALL VS MULTI-STEP — RECOMMENDATION (a decision, not a survey)

**RULED SHAPE: staged synthesis — FOUR calls (SO-S1-2 re-ratified at four with A3's refuter, founder 2026-07-16; body updated 2026-07-17 — it previously described the pre-A3 three-call draft)** along the data-dependency joints: **Call A = M2+M3** (attribution + assertions over the widened M1 record) → code validation (dangling-ID drops, the M2 weight law) → **Call B = M4+M5+M6** (contradictions + hypotheses + gaps over certified A output + merged m4c records) → **Call B′ = THE REFUTER (A3 APPROVED)** — the judgment stage runs a second time over the SAME certified inputs, framed as the strongest case against Call B's leading hypothesis. **CODE compares the two commitments (the leading-hypothesis labels): same leader → conviction recorded high; different leaders → the case is genuinely uncertain — the decision-confidence-class conviction ADVISORY degrades + admin flag. NEVER a verdict input** (additive storage field beside the synthesis record; feeds the future confidence-aware-autonomy routing for free) → S-0 certification + M5 code rules → **Call C = M7+M8+M9** (doubt + questions + snapshot over everything). Each call: pinned schema (H7 structured-outputs, fail-open), tolerant parser, per-stage persistence.
**Founder's re-ratification rationale, for the record (2026-07-17):** M5 is code-constrained to exactly one `leading` and M5 is never a verdict input, so backwards reasoning inside stage B produces a prettier narrative but cannot move the decision — blast radius is narrative-only, and per-stage artifacts make it visible when it happens.
**Why (against the criteria):**
- **Quality:** each stage's fixed move is enforced by a small schema; a single call over nine modules invites module bleed (M9 written first, modules back-filled as rationalization) — the exact failure the fixed-method law exists to prevent. Nine separate calls add nothing: M2/M3 (and M4/M5/M6) genuinely share context.
- **Verdict determinism:** unaffected by call count (the verdict reads only certified fields) — and the stage seam is what made A3 possible at all: the refuter is H7's consensus pattern lifted to the judgment layer, re-running ONE stage instead of the world; a single mega-call would have made it surgically impossible.
- **Cost:** ~4 synthesis-class calls/case vs 1 — bounded (A3 re-ratified: measured disagreement is worth the call), and the caching gate owns the margin lever. Not confirmed until measured: AT-SYN-COST records the delta (the AT-7/AT-A6 pattern) and feeds the caching gate.
- **Debuggability (decisive):** per-stage frozen artifacts mean a bad snapshot is traceable to the stage that broke — the same reason the track layer validates per-track instead of one mega-call. Also the backtesting (I1) unit becomes the stage, not the monolith.
G005's "decide empirically" is honored as a check, not a stall: if Call-B/B′/C quality on real cases contradicts this, the founder sees near-miss data at the ATs — the seam (four calls behind one `runSynthesis` signature) makes consolidation a one-site change.

## SIGN-OFFS / OQs / SEQUENCE (proposed boards — founder rules)

**Sub-gates, in order, never bundled (SEQUENCE RULED 2026-07-16: S-0 → S-2 → S-1; S-2 does NOT fold):** **S-0** firewall micro-gate — **✅ FROZEN 2026-07-17 at `11476f7` (full AT record in the status block)** → **S-2** versioning/hardening slot — **✅ FROZEN 2026-07-17 at `acef731` (full AT record + the two logged items in the status block; the `server-only` marker DECLINED, delivered as the derived client-boundary lock — see logged item (i))** — RULED SCOPE: ios model string from MODEL_CONFIG · synthesis_version pins · **replay-attempt.ts version-pin preflight (R1 — the canary amendment's item iii, already founder-approved; PRECONDITION to A5: AT-SYN-D and the backtest replay through that seam, and an unpinned seam would misattribute version drift as extraction noise, corrupting the exact signal the backtest exists to produce)** · PRICE_IN/OUT derived from MODEL_CONFIG (R3 — so AT-SYN-COST feeds OQ-S3 correct numbers) · `server-only` marker on lib/research/** (ADDENDUM-2 Move 6a, founder-placed here) → **S-1** contracts + M1-code + the staged engine (four calls incl. A3's refuter) + prompts, carrying A1/A2/A5/A6/A7/A8 + R2 + the four routed module laws; **A5 is an S-1 FREEZE CONDITION — the synthesis prompts do not freeze until the backtest over the frozen corpus has run and the founder has seen the data**; A4 held out pending its ruling; OQ-S1 and OQ-S4 block their modules → client-surface wiring stays OUT (the PDF/client gate owns G1–G3's activation).
**Sign-offs requested:** SO-S0-1 certification fn + all-sites composition · SO-S0-2 the structural-critical/load-bearing conditions (B1.2–3) · SO-S0-3 m4c merge law + m4c-1.1.0 additive bump · SO-S0-4 doubt locked out of the verdict in v1 · SO-S1-1 module contracts as written (B2) · SO-S1-2 the FOUR-call staging incl. A3's refuter (B7) · SO-S1-3 tier-seam mechanism with the mapping left UNRULED (B3) · SO-S1-4 Rider-2 proof plan incl. the delivery-scan extension + B4-EXT + the derivation rule (B4/G2). **BOARD STATUS — ✅ ALL FOUR SIGNED (founder, 2026-07-17; round 1 read at `ac94f1a` signed SO-S1-3/4 and returned SO-S1-1/2; round 2 read at `8a59011` signed SO-S1-1 with two conditions — M4's In carries the M1 record + OQ-S5 provenance flag, M7's honesty label — and SO-S1-2 as written. Round-1 record: SO-S1-1 was returned because A1/A8 had never reached B2's contracts (M1 Out "typed as today" · M2 In "M1 items" · M3 without `brand` · M7 gap derived from LLM-written M2/M3 fields, the exact thing A1 replaces); SO-S1-2 because B7 still said three calls against the ruled four.)**
**Open questions:** **OQ-S1 ✅ RULED (a) — founder, 2026-07-17: v1 cost-of-being-wrong = OBSERVABLE ENFORCEMENT STAKES ONLY** (Track 3 enforcement-posture signal + validated veto-grade keys present + breadth of brands at issue; NO intake field). Rationale on record: order value / account exposure are not collected, and adding an intake field is a product decision with a client-surface cost — it belongs at the client-surface gate where intake is already being touched, not smuggled into the engine build; and the axis stays HONEST — everything feeding it is something the system OBSERVED, never something the client ASSERTED (a self-reported order value would put an unverified claim inside the calibration of the module whose entire job is measuring unverified claims). **A2 UNBLOCKED — the founder authors the matrix; the skeleton's Axis-2 cells stay BLANK until he fills them.** (Was: BLOCKING for S-1 via the A2 ruling, 2026-07-16 — Module 7 cost inputs) · OQ-S2 does M9 retire the dead `what_to_verify` field or keep contract compatibility (rec: keep until the client-surface gate) · OQ-S3 synthesis model tier at go-live (config-only flip per runModel design; cost data from AT-SYN-COST informs it — no figure stated here; R3's PRICE_IN/OUT fix in S-2 is a data-quality precondition so OQ-S3 is ruled on correct numbers) · **OQ-S4 — C2, CROSS-BRAND AGGREGATION (routed from the Track 3 freeze "→ Synthesis Engine phase"; FOUNDER RULING REQUIRED — no aggregation rule is invented here, per the standing law).** What exists FROZEN today: aggregation happens at the SIGNAL level inside each track — per-brand vetoes/keys pool into one track signal, a veto on ANY brand hard-fails the whole track, and the verdict is per-case (worst-brand-governs by construction of the veto law). A8 partitions M3 assertions by brand; it does NOT answer how multi-brand findings aggregate into ONE M9 narrative and how the verdict-driving brand is attributed (the single_99(a) ruling lineage: attribute the flag to the BRAND, never the supplier). Candidate shapes for the ruling — (i) accept worst-brand-governs (the frozen behavior) with M9 required to NAME the driving brand and state the per-brand split; (ii) per-brand mini-snapshots inside M9 with one case verdict; (iii) anything that changes the verdict math — which touches frozen computeVerdict and is its own gate, flagged not recommended. **STOP: S-1's M9 cannot be finalized until OQ-S4 is ruled.** *(→ superseded — ruled, next sentence)* **OQ-S4 ✅ RULED (i) — founder, 2026-07-17: one case verdict (frozen behavior untouched) + M9 names the driving brand + carries the per-brand status; full contract statement, the describe-and-stop on the per-brand-status carrier, the re-homed option (ii), the scope note, and the Axis-2 coherence consequence all at B2/M9. PROCESS NOTE on the record (founder-ordered): this ruling was MADE 2026-07-17 and NEVER SENT — the build thread correctly reported OQ-S4 open every round while the planning thread read past it and asserted the board clear. A ruling that exists only in conversation is not a ruling — the chat-only-addenda class, third instance (addenda, Q4(b), now this). Planning thread's error. FOUNDER'S CORRECTION also on the record: OQ-S4 was framed as an open architectural fork ("the last architectural decision in the engine") — overblown; the verdict was already per-case by frozen construction and the narrative attribution was already ruled at single_99(a); option (i) is the frozen behavior plus an existing ruling. A question framed larger than it is costs the same as a claim broader than its code.** · **OQ-S5 ✅ RULED — NEITHER FLAGGED OPTION (founder, 2026-07-17): the conflict-merge half is CODE-ENFORCED at the VENDOR-DOCUMENT-CLASS granularity** — document-sourced contradictions contribute AT MOST ONE load-bearing record (**principle as CORRECTED by the founder 2026-07-17: the cap's subject is ARTIFACTS OF ONE ACT OF CLAIMING — the submitted paperwork; the vendor's website is a SEPARATE act and website-vs-observable contradictions must still floor**; the class is stored — `claimant: "vendor"` code-literal at track4's construction site, verified, and the ⟺-track4 equivalence is LOCKED at S-1 by a derived source-scan lock, not claimed). Prompt-only enforcement on a verdict path is the Keepa class — rejected. Two-sided obligation spec'd (web-vs-document contradictions UNAFFECTED, test-locked at S-1). Full amendment + mechanism + verified precondition at B2/M4. **RESIDUAL NOTED, open as a note:** the transaction field remains absent; a future too-coarse finding is a product change with its own describe-and-stop, never a quiet loosening. · **OQ-S6 — single_99 (b), THE VENDOR-DIRECTED ENFORCEMENT FACET (homed 2026-07-17 on founder confirm-check: the tracker routed it "PROMINENTLY FLAGGED for the tier-design/Synthesis phase, NOT accepted silently" and it was homed NOWHERE in this spec until now — only single_99(a)'s lineage appears, inside OQ-S4). A TIER-DESIGN question, ruled WITH the tier map (B3's UNRULED mapping), NOT ruled here and NOT blocking S-1: B3's mechanism already handles it structurally — an unassessed facet is a stated limitation, never a gap counted against the case. The substance (from the tracker, options already on the founder's record, none invented here): Track 3 correctly punts vendor-directed enforcement to Track 2 per Amendment 2, but single_99 has no Track 2 — a brand's C&D against THIS vendor is owned by NEITHER track. Tracker options: (i) Track 3 owns the facet on Track-2-less tiers · (ii) single_99 states it does not assess vendor-directed enforcement (honest scope limit) · (iii) single_99 always includes minimal Track 2. Founder leaned (ii) now / (iii) eventually — ruled with the tier design in hand.**
**S-1 BUILD SEQUENCE — ✅ RULED (founder, 2026-07-17): SIX SITTINGS, two changes from the build thread's five-sitting proposal (S-1a split into contracts-only + a separate M1-assembler sitting — a wrong contract is wrong in every later sitting, so types freeze before anything consumes them, and the assembler gets its own review, not one buried under a type review; A7's injection fixture lands in BOTH S-1c at the first LLM boundary AND S-1d at the poisoned-assertion seam — added, not moved). HARD RULE, ALL SIX (founder, verbatim): each sitting STOPS before its ATs; the founder runs them; the planning thread interprets and declares the freeze; Fable records. The loop does not change because the gate is big. TDD, RED first, frozen core byte-identical, describe-and-stop on migrations.**
- **S-1a — THE NINE CONTRACT TYPES ONLY:** renamed M2 fields · M3 `brand` · m4c-1.1.0 · both siblings (`brand_evidence_status`, `dimension_run_record`) · R2 schema. Types + shape locks, ZERO logic. Frozen before anything consumes them.
- **S-1b — THE M1 ASSEMBLER** against the frozen contracts: widened-record assembly, the Q3 hash byte-identity lock, the naming-law locks. Reviewed on its own.
- **S-1c — CALL A (M2+M3):** prompts + pinned schemas; M2 code locks (firewall-wins + weight pairing); M3 dangling-drops + A8 + the ROSTER LOCK; **A7 order-invariance lands here.**
- **S-1d — CALL B + B′ (M4+M5+M6):** m4c merge with `origin`; OQ-S5's cap + four two-sided ATs + the claimant-literal lock; M5 one-`leading`; M6 B3-filter + `dimension_run_record` consumption; A3's refuter with the code commitment-comparison. **A7's injection fixture ALSO lands here — the poisoned-assertion seam (a copy added to the first-boundary one, not moved from it).**
- **S-1e — CALL C (M7+M8+M9):** matrix d7-1.0.0 as versioned config + the Axis-1 fallback derivation; M8; M9 doubt-level shapes + the derivation-rule prompt law + the G2 scanner extension; `brand_evidence_status` assembly.
- **S-1f — A5 + A6 + THE AT BOARD:** AWI-2607-022's clean re-run FIRST (founder-owned) → the A5 backtest over the frozen corpus → **the founder sees the data → prompts freeze → `synthesis_version` bump + BOTH forward-pin updates in the freeze commit (the S-2 checklist item) → S-1 FREEZE.**
**NOT IN THIS ITEM:** agency override machinery (G005 item 7) · G6 memory read-side (seam only) · client-surface/PDF wiring of M9/M8 · Tech-Arch cascade (founder docs) · Keepa (inert; signal at its gate) · caching (its own pre-launch gate).

---

# PART C — STAGING→MAIN PROMOTION (founder-run; exact steps + rollback)

*(Also delivered in chat. All commands run from `D:\Projects\Hyprriq\portal`. Nothing here touches the database.)* — see the session message; steps recorded there verbatim so this spec stays engine-scoped.
