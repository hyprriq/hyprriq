# HyprrIQ — Session Handover (H1–H7 FROZEN → Spec-B gate → Track 3) · 2026-07-09

**Working dir:** `D:\Projects\Hyprriq\portal` (lowercase "iq"). **Branch:** `staging` (pushed through `8b60558`; `main` stale — promote before go-live).
**Read this file first — resume from "IMMEDIATE NEXT". Do NOT re-derive or re-litigate anything marked frozen/ruled.**
**⚠ THE ONE AUTHORITATIVE TRACKER: `docs/HyprrIQ_OPEN_ITEMS.md` IN THIS REPO — absolute path `D:\Projects\Hyprriq\portal\docs\HyprrIQ_OPEN_ITEMS.md` (git-tracked SSOT since commit `62e460d`). ALL reads AND writes use that path only. `D:\Projects\Hyprriq\Docs\` is ARCHIVE-ONLY — never read or write the tracker there.** Read the tracker second — its bottom section **"CURRENT STATE + SEQUENCE"** governs sequencing (planning intent; CODE is ground truth for what exists).

## ⛔ IMMEDIATE NEXT — spec the Spec-B domain-research UNDER-RESOLUTION gate (founder-review artifact, STOP before code)

H1–H7 are ALL FROZEN (H7 declared 2026-07-09, five ATs live-passed + two deferred-live by design). The Track 3 gate is OPEN, but the founder-endorsed sequence (tracker "CURRENT STATE + SEQUENCE" item 2) promotes the **Spec-B under-resolution gate BEFORE Track 3**: identity resolution (Track 0.5) is the weakest load-bearing layer — it keys the corpus, selects the research identity, and is what replay reuses — and it has a known false-negative class with no structural guard.

**Scope (from the tracker's KNOWN GAPS entry, logged 2026-07-06):** real, live domains (tdsynnex.com; globaldist.com twice) score `website_dead` in Spec-B domain research. Root-cause hypothesis: the `identityResolver` dominance threshold is tuned for NAME discovery (dominance must be earned) and is over-strict for DOMAIN research (the domain is already the anchor). Current behavior is SAFE (escalate-never-guess) but LOSSY. The gated item folds in: (1) threshold/branch treatment for domain subjects; (2) rename the client-facing `website_dead` note (a resolution failure must not read as "your supplier's site is dead"); (3) H2-style `llm_failed` instrumentation for Track 0.5 research calls (H2 scoped that to Tracks 1–2 only). Key code: `lib/research/track05.ts`, `lib/research/identityResolver.ts`. Track 0.5 is FROZEN — any touch needs explicit founder sign-offs in the spec (H4's SO-1 precedent: smallest possible touch, route INTO frozen logic). Two-sided ATs mandatory (fix the false-negative WITHOUT weakening escalate-never-guess for genuinely dead/ambiguous domains — Zzqxwv/AWI-2606-003 class must still escalate). Live fixtures exist: the tdsynnex/globaldist cases. This also unblocks H4's logged limitation (the live resolved-to-different-entity demo).
**Do NOT fold in:** Track 3 (next gate after this), Phase H PDF, confirmation loop, pre-launch security phase.

**Gate rhythm (unchanged):** spec/plan as founder-review artifact → founder approves + answers OQs/sign-offs → migrations written by Claude, RUN by founder (this item likely needs NO migration) → TDD build, commit per task, full verify (tsc+eslint+vitest+build) → push staging → **founder personally runs all ATs → phase frozen.** Never bundle phases. STOP before code at every gate.

## Where things stand (detail lives in the tracker's per-phase lines)

- **H1–H6 ✅ FROZEN** (truth&record · fail-loud · verdict semantics · identity coherence · client surface · money&corpus). **H7 ✅ FROZEN (2026-07-09)**: URL dedupe (pack **1.1.0**), corroboration breadth website/address_fraudulent (firewall **1.3.0**), source-diversity pass-cap (one shared fn, every signal site), hard-fail two-pass consensus (OQ-A: failed confirmation = veto kept + escalate), structured outputs capability-guarded fail-open (**live result: SUPPORTED**), replay seam (real attempts; hash-identity proven live on AWI-2607-021), firewall-registry coverage lock (Track 3/4 entry conditions enforced by failing test). PIPELINE_VERSION **1.1.0**. 466/466 tests. NO migration in H7.
- **Deferred-live register:** AT-2's live fraud-corroboration half + AT-4's live consensus exercise (no on-demand fraud/hard-fail fixture exists BY DESIGN — first real corroborated fraud exercises them; post-env-separation, a synthetic two-source fraud-pack replayed through the seam becomes the deterministic exercise — add to that phase's spec); 30-day outcome-checkpoint email ~2026-07-20 (Morendelli); rollover RPC on first real renewal.
- **Standing ritual:** canary panel — replay the same 3–5 frozen (delivered/terminal ONLY) attempts monthly via `scripts/replay-attempt.ts` (~$0.30), eyeball signal diff. NOTE: replaying a NON-delivered case updates its live verdict/status (OQ-D real-attempt semantics) — never canary a queued case.

## Standing rules (all founder-confirmed)

- **Frozen core (explicit sign-off required to touch):** `deriveTrackSignal`, `computeVerdict`, `weights.ts` scoring, 6-gate firewall LOGIC (config grows by SO — 1.3.0 current), Evidence Pack contract (**1.1.0** current; 1.0.0 packs frozen history), `applyVerdictCeiling`, `researchIdentityFor`, Track 0.5/identityResolver, publish-confirm, all frozen H1–H7 semantics.
- **Fixture rule (4×-earned):** select AT fixtures by DB mechanism, never label/verdict; verify a fixture's STORED class and LIVE class SEPARATELY (Zzqxwv: stored attempt-1 hard-fail for rejudge tests; live = deterministic absence-based soft-fail since the 2026-07-05 ADR-T1-001 prompt rules).
- Env file is `.env.local` (no `.env`). Founder runs everything touching prod (migrations, harness scripts, publishes, ATs). Claude reads prod DB read-only only.
- Pattern rules: any post-signal/post-verdict adjustment = ONE shared fn at every site; every new veto key needs collision audit + positive definition; two-sided tests (false-positive kill AND true-positive survival); absence of evidence ≠ fraud; flag bugs found while planning/building; stop if anything turns architectural.
- Tooling: vitest 4.1.9 fails tests where a mock-originated rejection escapes an imported module (logged; two-file repro exists) — don't fight it, note affected contracts in code comments.

## Key artifacts
Tracker (SSOT): `docs/HyprrIQ_OPEN_ITEMS.md` (this repo — see path rule above). Plans: `docs/superpowers/plans/2026-07-05-h1…h4`, `2026-07-06-h5-client-surface.md`, `2026-07-06-h6-money-and-corpus.md` (OQ-1 rulings + addendum inline), `2026-07-07-h7-firewall-hardening.md` (SO/OQ rulings inline). Founder-run scripts: `rejudge-case.ts` (zero-API determinism) · `replay-attempt.ts` (frozen-evidence re-judge) · `rerun-batch.ts <case-id…> [--run]` · `cleanup-corpus.ts` · `check-structured-outputs.ts` · `validate-attribution.ts`. Supabase `mjkacjrrrmlwlwkienvq.supabase.co` (single project = prod; env separation is the pre-launch security phase). Inngest app `hyprriq` (3 functions: pipeline, watchdog, outcome-checkpoints). Staging URL `hyprriq-git-staging-hyprrx-hyprriq.vercel.app`.
