> ⛔ **SUPERSEDED 2026-07-19 — DO NOT WORK FROM THIS FILE.** Both rulings landed, Steps 3 + 4 built at `875f158`, and **S-1 IS FROZEN**. Current handover: **`docs/HANDOVER_S1_FREEZE.md`**. Text below preserved verbatim as the record of the mid-sitting stop (correction discipline: mark, never delete).

# HyprrIQ — Session Handover (S-1f mid-sitting: backtest delivered, TWO founder rulings pending → Step 3 + Step 4 → S-1 FREEZE) · 2026-07-19

**Working dir:** `D:\Projects\Hyprriq\portal`. **Branch:** `staging` (pushed through the flip-content commit `b587a42` + this handover). **Model note:** next session may be Opus 4.8 or Fable — nothing here is model-specific; follow the artifacts, not memory.
**Read this first, then the tracker (`docs/HyprrIQ_OPEN_ITEMS.md` — THE SSOT), then the two ruling artifacts (below). Do NOT re-derive or re-litigate anything frozen/ruled. The status chains in the tracker + gate spec are authoritative and current.**

## ⛔ WHERE WE ARE — STOPPED, BY DESIGN, ON TWO FOUNDER RULINGS

S-1 (the synthesis engine, ADR-G005) is at its LAST sitting, S-1f, which is mid-flight:
- **Step 0 ✅** — AWI-2607-022 re-run clean (attempt 6 live, artifact superseded).
- **Step 1 ✅ FROZEN** (founder-declared at `d82248b`) — the four calls WIRED behind `runSynthesis`; stub retired; memo DISABLED (Q4(b), source-scan-locked); regex divergence closed by a cross-file identity lock; four-delivered-case reproduction proof EXACT (zero wiring drift; 001's exit-1 = the ruled AT-S0-2 pre-existing state).
- **Step 2 ✅ DELIVERED** (`b46e280`) — the A5 backtest: 66 frozen attempts, synthesis calls only, memo off, DB read-only, $6.97 (AT-SYN-COST). Table + distribution in `docs/superpowers/plans/2026-07-19-a5-backtest.md`; the standing harness is `scripts/backtest-synthesis.ts` (DRY default, `--run` fires).
- **⛔ STOPPED — the two rulings the founder must make before ANYTHING moves:**
  1. **THE FLIP RULING.** 4 attempts flip usable_with_conditions → verify_before_purchase through the ≥2 load-bearing floor (018#9, 021#8, 022#2, 022#6 — ALL non-delivered/pointer attempts; ZERO delivered flips; direction uniformly toward caution). Content laid out client-plainly in `docs/superpowers/plans/2026-07-19-a5-flip-contradictions.md` + the final session messages. The decision shape: **021#8** = 2 real, distinct-source, analyst-credible contradictions, fired on BOTH runs, zero audits (the strongest "designed mechanism" case) · **022#2** = 1 real + 1 scope-limitation-dressed-as-contradiction (same evidence ids on both sides — without it the floor doesn't fire) · **018#9 + 022#6** = BOUNDARY-UNSTABLE: did NOT reproduce on re-run (018#9's records all S-0-clamped as not structurally earned — one side of each pair was the submission's own premise citing no evidence; 022#6 emitted zero contradictions). **The boundary-instability finding matters beyond the flips:** identical frozen evidence → different verdict across runs at the floor margin, gated only by citation-resolution luck — AT-SYN-D-relevant; the founder may rule on it together with the flips (e.g., accept / require stability / add a code guard — HIS call, nothing proposed).
  2. **STOP #1 — THE GAP-THRESHOLD RULING.** Distribution under the FLAGGED TEST_ONLY thresholds (narrow≥1, material≥3, wide≥6 over unresolved+unknowns): minimal=10 · targeted=6 · elevated=40 · broad=10. Raw gap totals range ~1–20 (real attempts carry 3–14 stored unknowns, so wide≥6 sweeps 50/66). Axis-2 lands `low` on 59/66, `significant` on 7, zero `severe` (the G4 flag visible live). B′ disagreed 20/66 (~30%). Cap fires: 0. The founder rules narrow/material/wide from the table — **his authorship, like the cells; do NOT propose numbers.**

## THEN — THE REMAINING WORK (fully specified; do not reorder)

- **STEP 3 — A6 watch conditions** (write-side, unbackfillable): per-hypothesis structured watch conditions + prediction_correct scoring hooks, active from the next live case. If it needs an additive `case_outcomes` column → **DESCRIBE AND STOP** (founder runs every migration). TDD, stop before ATs.
- **STEP 4 — THE FREEZE COMMIT** (only after BOTH rulings): (a) founder-ruled gap thresholds replace `SYNTHESIS_GAP_THRESHOLDS` (currently = TEST_ONLY, loudly marked INTERIM in `lib/research/synthesisEngine.ts`); (b) `synthesis_version` bumped off "0.0.0" to its S-1 value **+ BOTH forward pins (scripts/rerun-batch.ts, scripts/dispute-rerun.ts) updated IN THE SAME COMMIT** (the S-2 checklist law — both scripts STOP until repinned, by design); (c) whatever the flip ruling requires; (d) full suite unpiped $LASTEXITCODE 0 · tsc · frozen-core diff.
- **STOP #2** before the final AT board — founder runs it, the planning thread declares the **S-1 FREEZE**, the build thread records.
- **HARD RULE all steps:** TDD RED-first with WATCHED-FAILS (a lock that has never failed is not a lock — every sitting this gate proved one); stop-before-ATs; frozen core byte-identical; describe-and-stop on migrations/frozen surfaces; claims about repo state verified FROM SOURCE; a ruling that exists only in conversation is NOT a ruling (three chat-only instances caught this arc); corrections preserve the original text and mark it, never delete.

## OUTSTANDING FOUNDER ACTIONS (his ledger)

1. The two rulings above. 2. **The additive migration (non-blocking, Supabase dashboard):** `ALTER TABLE case_synthesis ADD COLUMN synthesis_extension jsonb;` — until it lands, extension persists drop LOUD (audit-logged, H2 OQ-2 pattern; `persistSynthesisExtension` in lib/data/synthesis.ts). 3. staging→main promotion (steps delivered long ago; still pending). 4. Canary panel first monthly run (all four amendment items implemented). 5. skills-lock.json + untracked folders (backups/, codex-fresh-design/, mockups-codex-exploration/) rulings. 6. BUG-1 live check. 7. **Morendelli 30-day outcome-checkpoint email ~2026-07-20 — that is NOW.** 8. Keepa gate: DEFERRED until after the S-1 freeze (rulings 1–7 recorded in the tracker's DEFERRED section; $149 includes Keepa; client supplies ASIN/link; the three dormant Keepa weight keys flagged UNRULED for removal at that gate).

## FROZEN — NEVER TOUCH WITHOUT SIGN-OFF (additions this arc in bold)

Everything through S-0/S-2 (see tracker) + **S-1a contracts (`530881b`: the nine module types in contracts.ts, naming law, both siblings, R2 record)** · **S-1b `m1Assembler.ts` (`bd5402c`: Q3 hash byte-identity)** · **S-1c `synthesisCallA*` (`af04420`: firewall-wins, roster lock)** · **S-1d `synthesisCallB*` (`e30808a`: OQ-S5 cap + claimant-literal lock, one-leading, B3/financial filters, B′)** · **S-1e `synthesisCallC*` + `doubtMatrix.ts` + `synthesisMethodScan.ts` + the G2 review-route extension (`2ea4893`: founder's law shapes, d7-1.0.0 cells, derivation scanner)** · **S-1f Step 1 wiring (`d82248b`: runSynthesis, stageSynthesis+signals, memo-disabled)**. Suite currently **780/780 unpiped exit 0**. Versions: PIPELINE 1.7.0 · VALIDATION 1.7.0 · synthesis **0.0.0 (bump = Step 4, with both pins)** · rubric g003-1.1.0.

## KEY IN-CODE FLAGS THE NEXT SESSION MUST KNOW

- `SYNTHESIS_GAP_THRESHOLDS` (synthesisEngine.ts) = TEST_ONLY values, marked INTERIM — Step 4 replaces with the ruling. `TEST_ONLY_GAP_THRESHOLDS` (synthesisCallC.ts) is the test fixture; importing it outside tests is a defect.
- `VERDICT_SENTENCES` + the M9 limitation sentences + shape glue = **PROPOSED admin-side literals** — exact client strings are founder-ruled at the client-surface/PDF gate.
- The FINANCIAL_SCOPE regex exists in TWO frozen files (callB + callC), kept identical by the cross-file lock in `s1f.wiring.test.ts` — consolidation only at a gate that unfreezes one.
- b2b advisory metadata is NOT reconstructed by the backtest harness (feeds no axis); M1 diversity carriers ride empty on live wiring v1 (stored per-track rows still carry source_diversity) — both flagged, neither verdict-relevant.
- The four delivered-case rejudge ids: 001 `a1ee6ef3-6527-47fe-8050-0bb467528e20` (expected exit 1, ruled pre-existing) · 021 `2b359a6a-98f9-49c9-8f57-c19f4d8daaac` · 023 `6c4ad68d-b2a7-446a-9064-e60f0683c13c` · 031 `5f6a093f-d3f8-4675-be4e-4452b931a80e`. 022 case id `1e13b79e-09ce-4100-abf2-4b1333b449c5`.

## AFTER S-1 (ruled sequence — do not resequence)

Client-surface/PDF gate (owns M9 activation, G1–G3, exact client strings, per-brand-status rendering = client-value #7, OQ-S2/S6) → pre-launch security phase (env separation, RLS, virus scan, PRICING RECONCILIATION go-live blocker) → caching/ADR-008 (memo returns keyed on FULL synthesis input; rollups-follow-adoption question) → Keepa gate (post-S-1, founder-ruled deferral; consult the Synthesis engine on its own integration). Tier map (B3) UNRULED-EMPTY; single_99(b)=OQ-S6 rides tier design.

## STANDING (compressed — full versions in tracker/memory)

LLM proposes code decides · absence ≠ fraud · Modules 1–8 never reach any external party · no threshold tuning to force a result · founder runs everything touching prod; build thread reads prod READ-ONLY only · `.env.local` env file · planning-thread errors get flagged not reconciled (Track-2/3, "two calls", "call_b_prime" precedents) · the founder's mandate: verify his instructions from source, challenge errors plainly, proceed autonomously on reversible non-gated ground · doubt is calibrated trust in a stated conclusion; confusion is the absence of one.

## KEY ARTIFACTS (all in-repo)

Gate spec (all rulings + the full status chain): `docs/superpowers/plans/2026-07-16-synthesis-engine-gate.md` · Tracker SSOT: `docs/HyprrIQ_OPEN_ITEMS.md` · **The two ruling artifacts: `2026-07-19-a5-backtest.md` (the 66-row table + distribution) and `2026-07-19-a5-flip-contradictions.md` (the four flips' certified content + re-run stability)** · doubt matrix (filled, d7-1.0.0): `2026-07-16-doubt-matrix-skeleton-UNFILLED.md` (filename historical; content governs) · ADDENDUM-1 (A1 split ruling): `2026-07-16-synthesis-addendum-1-algorithm-amendments.md` · prior investigation chain: corroboration-gate investigation, r-axis enumeration, claimant sizing, source-type sizing, brand-status carrier report, doubt-matrix corpus pull (all `2026-07-17-*`) · UI/UX tracker (Keepa ASIN field entry): `D:\Projects\Hyprriq\Docs\HyprrIQ_UIUX_TRACKER.md`.

**NEXT SESSION'S OPENING MOVE:** read this file → read the tracker's S-1 status chain → hold at the stop until the founder delivers the flip ruling + threshold ruling → record them (gate spec + tracker, correction discipline) → execute Step 3 then Step 4 exactly as specified above → STOP #2 → the planning thread declares the S-1 freeze → write the S-1 freeze record + the next handover. If the founder's first message IS the rulings, verify any repo-state claims from source before recording, then proceed.
