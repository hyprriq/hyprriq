# HyprrIQ — Session Handover (S-0 frozen · S-2 built → S-2 ATs → S-1 gate) · 2026-07-17

**Working dir:** `D:\Projects\Hyprriq\portal`. **Branch:** `staging` (pushed through `acef731`; `main` = create-next-app scaffold, promotion instructions already delivered — see LEDGER item 4).
**Read this first, then the tracker — resume from "IMMEDIATE NEXT". Do NOT re-derive or re-litigate anything frozen/ruled.**
**⚠ THE ONE AUTHORITATIVE ENGINEERING TRACKER: `docs/HyprrIQ_OPEN_ITEMS.md` IN THIS REPO (git-tracked SSOT).** The planning set in `D:\Projects\Hyprriq\Docs\` (incl. ADR-G004 master = `hyprriq_intelligence_engine_master.md`, ADR-G005, ADR-G006) is product-level orientation ABOVE it, read-only; on any engineering contradiction THE TRACKER WINS — flag, never reconcile silently. **FROZEN STATE WINS over every planning doc** (the A4-answer discipline: every G005/G004 divergence is surfaced, never silently reconciled).

## ⛔ IMMEDIATE NEXT

1. **Founder runs the S-2 ATs** (his action): `npx vitest run lib/research/s2.locks.test.ts lib/research/versionDelta.test.ts` → 11 passed · `npx vitest run` → **697/697, $LASTEXITCODE 0 UNPIPED** · `Select-String -Path scripts\rerun-batch.ts, scripts\dispute-rerun.ts -Pattern "synthesis_version"` → both pins at "0.0.0" · OPTIONAL live (c): `npx tsx --env-file=.env.local scripts/replay-attempt.ts 2b359a6a-98f9-49c9-8f57-c19f4d8daaac 6` → the ⚠ VERSION DELTA — ATTRIBUTION RECORD block prints and rides the audit marker (costs a replay + appends an attempt to a frozen-pointer delivered case; equally valid to defer to A5's backtest). **The planning thread declares the S-2 freeze — record it in spec + tracker when declared.**
2. **Then the S-1 gate — BLOCKED ON FOUNDER RULINGS, no S-1 code until they land:** sign SO-S1-1..4 (module contracts as amended · FOUR-call staging per A3 re-ratification · tier-seam mechanism with the mapping EMPTY · Rider-2 proof plan incl. the delivery-scan extension G2) · rule **OQ-S1 (BLOCKING** — Module 7 cost-of-being-wrong inputs; rec (a) observable enforcement stakes only) · rule **OQ-S4** (C2 cross-brand aggregation — candidates on record in the spec, NOTHING invented; M9 cannot finalize without it) · **founder fills the doubt matrix** (`docs/superpowers/plans/2026-07-16-doubt-matrix-skeleton-UNFILLED.md` — cost axis renamed low|significant|severe; Axis-2 inputs wait on OQ-S1) · **A4 stays HELD** (Q2 answered: narrative-only is the only S-0-compatible reading; mechanism unruled; "routes to VBP" language retracted).
3. **S-1 build (when opened) carries:** A1 (with BOTH discharged preconditions as build law: Q3 — `computeEvidenceHash` projection stays BYTE-IDENTICAL, widened M1 record rides BESIDE the hash; Q4 RULED (b) — synthesis **memoization DISABLED at S-1**, B6's by-construction claim already retracted, memo returns only at the caching gate keyed on the FULL synthesis input) · A2 (founder-authored matrix as versioned config — Move 1 reduced form instance #1) · A3 (refuter pass, FOUR calls) · **A5 as an S-1 FREEZE CONDITION** (backtest over the frozen corpus runs and founder sees the data BEFORE the synthesis prompts freeze; its precondition R1 landed in S-2 ✅) · A6 (per-hypothesis watch conditions; possible additive case_outcomes change = DESCRIBE-AND-STOP) · A7 (order-invariance + injection ATs) · A8 (brand scope on M3) · R2 (persist schema_fallback per stage) · the FOUR module laws (cross-track standpoint → M9 · financial-scope → M6+M8, the 11,300-EUR air-freight flag is the named pattern to remove · document-diversity → M4 · OQ-S4 governs aggregation) · tier map UNRULED-EMPTY (mechanism consumes what ran; sole source = the plan registry once ruled).
4. Sequence after S-1: client-surface/PDF gate owns Module 9 activation (G1 ownership gate + G2 scanner extension + G3 pinning + sentinel ATs) → pre-launch security phase (env separation, RLS, virus scanning, PRICING RECONCILIATION — reclassified GO-LIVE BLOCKER) → caching/ADR-008 (entry conditions recorded: full-input memo key; rollups-follow-adoption question).

## FROZEN (founder-declared; never touch without explicit sign-off)

H1–H7 · SB-1 · SB-2 · PG-1 · Track 3 · SB-3 · **Track 4 (2026-07-13 at f153fd2, live-earned on AWI-2607-031)** · **Track 5 (2026-07-14 at 2b9126e — non-voting proven live)** · **F-spec sitting (2026-07-15: F1–F7 + ruled Track 5 copy "Consistency check — informational; does not affect the verdict")** · **S-0 (2026-07-17 at 11476f7 — full AT record in the gate spec; AWI-2606-001 mismatch RULED PRE-EXISTING).** 🏁 All six tracks (0–5) frozen — the evidence layer is complete.
**Frozen core (sign-off to touch):** `deriveTrackSignal`, `computeVerdict`, `weights.ts`, firewall config (VALIDATION **1.7.0** — the S-0 certification IS firewall config), Evidence Pack 1.1.0, `identityResolver.ts`, `researchIdentityFor`, `applyVerdictCeiling` (SO-5 KEEP), `applyDocumentationNoOverride`, **`certifySynthesisForVerdict` + the source-scan lock (derived callers, entry-symbol obligations, exemptions justified)**, the 794ed73 restoration, empty-set→soft_fail floor, publish-confirm, credit model, F2's findings ALLOWLIST (future fields private by default), m4c-1.0.0 writer (reader m4c-1.1.0 additive). Versions: **PIPELINE 1.7.0 · VALIDATION 1.7.0 · pack 1.1.0 · rubric g003-1.1.0 · IOS.synthesis_version 0.0.0 (pinned in rerun-batch + dispute-rerun)**. Suite **697/697** (unpiped exit 0).

## STANDING RULES (all founder-confirmed; the new ones this arc marked ★)

- Gate rhythm: spec → founder signs SOs/rules OQs → TDD build (RED first, genuinely — a lock that has never failed is not a lock) → full verify → push staging → founder personally runs ATs → **the planning thread declares freezes, never the build thread**. Never bundle; riders are BANNED (that ban is why doubt is locked out of the verdict).
- ★ **SO-S0-4 freeze condition (verbatim):** "any future attempt to make doubt a verdict input is its own founder-gated spec with its own certification. Never a rider. Never a follow-on commit."
- ★ **Explainability rule:** every "make the report more transparent" idea goes through a founder ruling — explainability features are method leakage by another name.
- ★ **Claims about repo state are verified FROM SOURCE before they are ruled on.** This session's earned lesson: two addenda were reported as committed when they were chat-only — name what exists, never let the claim outrun the artifact (the class was caught FIVE times this arc: commit-message naming, AT-S0-4's remembered file list, rejudge printing PASS on a skipped comparison, the piped-vitest exit mask, the addenda).
- ★ **Never gate a commit on a piped test run** — capture the exit code unpiped.
- ★ Rejudge semantics: default targets the DELIVERED attempt; a skipped verdict comparison prints TRACKS-ONLY PASS + SKIPPED and exits 2 — the full determinism claim may not print when the property wasn't tested. Canary amendment item (iv).
- ★ Replay preflight = ATTRIBUTION, NOT PREVENTION (R1 ruled): version delta printed + written to the audit marker on every replay; never a hard stop (rerun-batch keeps its STOP — money under changed rules).
- Fixture rules (7 earns): select by DB mechanism, never labels; STORED vs LIVE class separately; deliberate brands on confirmed test submissions; **replay only DELIVERED attempts** (AWI-2607-022 is still a replay artifact needing the founder's clean re-run).
- Client copy: notes state OUR limitation, never supplier conclusions; founder rules every exact client string (literal-locked). Infra failure carries no client note. LLM proposes, code decides. Absence ≠ fraud. Modules 1–8 never reach any external party. No threshold tuning to force an AT — return near-miss data. "Not confirmed" is a valid answer; never invent.
- analyst_reading quartet + sourcing_logic block + every new synthesis narrative field = ADMIN-ONLY until the client-surface gate (OQ-D lineage; B4's proof table governs).
- Keepa keys stay inert; signal the founder at its gate; never integrate without his key. Env file `.env.local`. Founder runs everything touching prod (migrations, replays, re-runs, publishes); Claude reads prod DB read-only only. Dispute re-runs: `scripts/dispute-rerun.ts` (no pointer advance, no recharge, ledger-tagged dispute_rerun, rollups untouched).
- Tooling: vitest doesn't typecheck (type-RED needs tsc); PowerShell here-strings choke on embedded double quotes in commit messages (use Bash heredoc); UTF-8 docs via Read/Edit tools, not Get/Set-Content.

## THE FOUNDER LEDGER (his actions, standing)

1. S-2 ATs (IMMEDIATE NEXT 1) → S-2 freeze declaration. 2. S-1 rulings package (IMMEDIATE NEXT 2). 3. AWI-2607-022 clean re-run (still a replay artifact). 4. **staging→main promotion** — strongest-ever stable point; exact step-by-step + rollback already delivered (Part C, session 2026-07-16; fast-forwardability verified: main `c9f1934` is an ancestor). 5. Canary panel first monthly run — amendment items (i)–(iv) now ALL implemented (rejudge pairing ✅, double-replay floor ritual, version-pin/delta ✅ S-2, delivered-attempt targeting ✅). 6. skills-lock.json (+90 lines from the 2026-07-14 plugin sync) — commit or discard. 7. Untracked folders ruling (backups/, codex-fresh-design/, mockups-codex-exploration/). 8. BUG-1 live check (dashboard Delivered tile → delivered list → View). 9. Morendelli 30-day outcome-checkpoint email ~2026-07-20 (first live H6 cron exercise). 10. Track-5 client-copy: founder may also rule whether the Sourcing-Logic card shows at all client-side (client-surface gate).

## OPEN FINDINGS / DISCUSSIONS PARKED

- **The all-n_a finding (own item, 2026-07-17):** delivered AWI-2606-001 carries source_clear on an all-n_a scoring set — pre-H3 engine cleared a supplier it never assessed (H1-frozen, zero exposure). Feeds the CONFIDENCE/ABSTENTION discussion with A4 (held) + the all-n_a→do_not_rely semantics note.
- ADDENDUM-2 dispositions: Move 1 reduced form (trade-secret inventory doc = anytime docs task; params-as-config = S-1 native) · Move 6a delivered as the derived client-boundary lock — **npm server-only constraint on record** (throws on plain-Node import; founder may override accepting --conditions churn on every script).
- Pricing = GO-LIVE BLOCKER at the security/Phase-J gate (one source of truth + test + Stripe price-ID reconciliation).
- single_99 vendor-directed-enforcement facet (tier design) · rollups-follow-adoption (caching gate) · normalizeName weakness = G6 entry condition.

## KEY ARTIFACTS

Tracker (SSOT): `docs/HyprrIQ_OPEN_ITEMS.md`. **Synthesis gate spec (all rulings inline): `docs/superpowers/plans/2026-07-16-synthesis-engine-gate.md`** + **ADDENDUM-1** (`...-synthesis-addendum-1-algorithm-amendments.md`, RULED: 7 approved/1 held, Q3+Q4 discharged on A1) + **ADDENDUM-2** (`...-addendum-2-defensibility-posture.md`, RULED) + **doubt-matrix skeleton** (`...-doubt-matrix-skeleton-UNFILLED.md`, founder fills). Prior gate specs under `docs/superpowers/plans/`. Founder scripts: `rejudge-case.ts` (delivered-attempt default; exit 0/1/2) · `replay-attempt.ts` (FOUNDER-RUN; version-delta attribution) · `rerun-batch.ts` (pins: VALIDATION 1.7.0 + synthesis 0.0.0) · `dispute-rerun.ts` (same pins; --reason mandatory; DRY default) · `cleanup-corpus.ts`. New this arc: `lib/research/synthesisFirewall.ts` (+ derived source-scan lock in its test) · `rejudgeReport.ts` · `versionDelta.ts` · `rederive.ts` · `modelFor()`/`priceFor()`. Supabase `mjkacjrrrmlwlwkienvq` (single project = prod until env separation). Staging URL `hyprriq-git-staging-hyprrx-hyprriq.vercel.app`. Delivered corpus (rejudge ids): AWI-2606-001 `a1ee6ef3-6527-47fe-8050-0bb467528e20` · AWI-2607-021 `2b359a6a-98f9-49c9-8f57-c19f4d8daaac` · AWI-2607-023 `6c4ad68d-b2a7-446a-9064-e60f0683c13c` · AWI-2607-031 `5f6a093f-d3f8-4675-be4e-4452b931a80e`.
