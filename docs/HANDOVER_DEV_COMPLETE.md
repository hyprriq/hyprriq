# HyprrIQ — Session Handover · DEV LANE COMPLETE, GAP AUDIT DELIVERED, UI/UX LANE OPENS · 2026-08-07

**Working dir:** `D:\Projects\Hyprriq\portal`. **Branch:** `staging`, HEAD `ef31b92` (pushed; Vercel auto-deploys staging). **Model note:** nothing here is model-specific; follow the artifacts, not memory.
**Read order:** this file → `docs/HyprrIQ_OPEN_ITEMS.md` (THE SSOT) → `docs/ADMIN_FOUNDATIONS.md` + `docs/SAAS_ARCHITECTURE.md` addendum §A–H (settled architecture) → `docs/UIUX_SESSION_PROMPT.md` (the design-thread standing brief) → `docs/HyprrIQ_OPEN_ITEMS_HISTORY.md` (the WHY).
**Gates at HEAD: 1076/1076 unpiped `$LASTEXITCODE` 0 · tsc 0 · eslint 0 errors · build clean · frozen engine byte-identical** (the arc's only frozen touches were founder-ordered and named: registry `ALL`/`GROWTH_SCALE` + `CATEGORY_PLANS` for the $149 tier; the documentPack comment correction).

## ⚡ WHERE IT STOPPED

The founder declared the dev lane finished and ordered a READ-ONLY GAP AUDIT before starting the UI/UX design lane. **The audit was delivered in-chat (2026-08-07) and its findings are the freshest conversation-only knowledge — they are preserved in full in §5 below.** The founder's next move is choosing: (a) run the recommended ~1-day PRE-DESIGN DEV BATCH (§5 blocks-design items), then open the design lane; or (b) open the design lane directly with the blocks-design list as design inputs. **No build is in flight. Nothing is half-committed. The working tree carries only the founder's standing untracked folders (`backups/`, `codex-fresh-design/`, `mockups-codex-exploration/`) + `skills-lock.json`.**

## 1. WHAT THIS ARC BUILT (2026-07-30 → 2026-08-07, every commit that matters)

- `ddf7caa` — Option-B super-admin ruling RECORDED in tracker; seed CONFIRMED RUN (probe: 1 row, master Clerk id `user_3FMpveJshdQq9bDAzxygPyPaMy2`, super_admin, enabled); §4 reconciled; STOP-2/3 added as forks (later ruled).
- `9313861` — **/admin/users crash fixed** (client bundle pulled `supabaseAdmin` via a CAPABILITIES value-import): dependency-free `lib/auth/capabilities.ts` + `import "server-only"` poison on `lib/supabase/admin.ts` (client inclusion now FAILS `next build`, proven by reintroduction) + `lib/auth/clientBoundary.lock.test.ts` (walks every "use client" value-import graph). CONSEQUENCE: founder tsx scripts need `--conditions=react-server` (full template in §7).
- `0741ce2` — **Dispute-rerun button on delivered cases** (reuses the existing frozen-delivered API path; own confirm; `rerun`-gated server-side).
- `591b818` — **Final client-dev batch:** ASIN intake (guard `lib/portal/asinIntake.ts`; threading via `lib/research/intakeExtras.ts` intersection type — frozen contracts untouched) · BUG-2 credits fix (`lib/portal/creditsDisplay.ts`, one honest computation for all 4 render sites, test-locked vs "7 of 5") · subscriber tier-switch webhook half (price → `planForPriceId` → plan_type + first-ever upgrade/downgrade events) · disabled client "Download PDF" stub.
- `c6c3ea2` — **Option A + riders (founder-ruled):** `raise_credits_to_allotment` RPC (GREATEST, atomic) · RIDER 1 once-per-cycle farm guard (`lib/billing/upgradeGrant.ts`, keyed on billing_audit grant-note prefix since `current_period_start`; second same-cycle upgrade calls NO rpc — test-proven) · RIDER 2 recorded (no immediate clawback; renewal rollover clamp is the delayed clawback) · `activatePlan` aligned to GREATEST (legacy-SET fallback pre-migration) · `creditsView` uses `credits_used_this_cycle`, never inferred · Model 2 (capability = active plan, stamped per case) RECORDED.
- `b682173`/`c97963a` — Rulings recorded: Modes A/B → client-surface gate SPEC-FIRST (mechanism done: dispute-rerun→H1→reinvestigation_pending IS the enforcement point); ASIN optional; dimensions-grid deferred with lean; **ADR-008 corpus caching DEMOTED to post-launch with the drop NAMED** (tracker 6.13; Q4(b) `synthesis_input_hash` constraint + F5 flag travel with it).
- `3531095` — `docs/UIUX_SESSION_PROMPT.md`: the design-thread standing brief (mandatory skill roster, layouts-before-code, design laws, token system, surface map). Design sessions start with: "Read docs/UIUX_SESSION_PROMPT.md and follow it. Today's target: X."
- `e077ade` — **ADMIN FOUNDATIONS:** staff invitations engineered (admin_invitations + Resend send via the gated notify helper + Clerk-verified-email claim on first admin visit in `requireAdmin`; sub_user-only minting; revoke API) · **CLIENT PARTITIONING** (staff_client_assignments; requireAdmin returns `clientScope`; list reads scope-filtered; detail/review pages 404 out-of-scope; `clientInScope`/`caseInScope` guards on notes/credits/review/questions/outcome/delete APIs; FAIL-CLOSED test-locked) · `getClientAccounting` per-client money read · assignments API (super-only) · `docs/ADMIN_FOUNDATIONS.md` incl. deferred DDL designs (acquisition/bulk/brand-DB §7).
- `a10cc81` — **Operator-aware AdminShell:** role-derived badge (Founder/Staff) · `navFor(operator)` in `lib/admin/nav.ts` consuming the SAME `can()/canManageUsers()/canManageStaff()` as the API gates (absent-not-disabled; emptied sections drop) · `/admin/support` read-only page + SCOPED badge (fail-closed: empty scope = zero rows/count, DB never queried — test-proven) · user prop restored on users/run pages.
- `713b06a` — **PERMISSION HIERARCHY (super_admin > admin > sub_user):** grant core `lib/auth/grants.ts` — containment rule (a) never grant what you don't hold (`grantableBy` intersects grantor's own), rule (b) granting power never grantable (roles only via `rolesCreatableBy`; invitations claim-mint sub_user only); SUPER_ADMIN_ONLY_CAPS (`adjust_credits`, `view_all_clients`) grantable by NOBODY, with a READ-TIME STRIP in `getOperator` (hand-edited rows inert); two-move escalation closed end-to-end in `grants.test.ts` · `DELIVERY_SLA_HOURS=1` + `REFUND_WINDOW_DAYS=14` constants · **refund policy LOCKED-captured** in ADMIN_FOUNDATIONS §9c (build deferred to billing pass) · stubs FILLED (outcomes aggregate, revenue over real rows, `/admin/clients/[id]/accounting`, honest prompts/settings text) · SHELLS (acquisition super-only static, bulk static, brand/supplier DBs live read-only over `vendor_intelligence`/`intelligence_events`) · nav Intelligence/Growth sections (gates = UNRULED defaults, flagged in nav source).
- `687b70a` — **FINISH-OFF (pricing ladder, all founder-ruled 2026-08-07):** brand caps **3/3/5/5** (dated re-ruling) · upload caps **0/2/2/2** (`MAX_CASE_DOCUMENTS=2`, operational-not-differentiator, silent guardrail; $99 upload DISABLED-with-upsell "Document review is included from the $149 report up" + server-rejected pre-charge) · **`KEEPA_LIVE=false`** single-flag ASIN gating (NO ASIN field renders anywhere until the flag flips; eligibility map ready: single_149+scale_499) · **`single_149` TIER BUILT** (1 credit · 3 brands · all 5 areas via the named frozen registry edits · category compliance YES, growth DELIBERATELY excluded · uploads 2 · standard SLA · no rollover/top-ups) · form-is-authoritative copy (PO/letterhead line; brandHelper false-promise corrected; Doc-Review descriptions → entity/address honesty; help FAQ rewritten) · every new client string in the banned-language must-pass fixture same-commit.
- `ef31b92` — **CLEANUP:** **Verified/Assessed wired READ-SIDE** (all 7 `finding_certainty` write sites are FROZEN and hardcode "unknown" — pipeline.steps ×6 + categoryStep ×1; write fix = named founder-run engine pass; the RULED derivation lives in `lib/portal/certainty.ts` — Verified iff ≥1 evidence item LLM-certainty "verified", else Assessed, absence never doubt — computed in the client projection, evidence consumed server-side never returned; `confidence_band` STRUCTURALLY removed from the client Finding type — the term-collision path is gone; badges two-value vocabulary, Unconfirmed/Inferred retired; old delivered rows render correctly, NO backfill needed) · **UPLOAD SECURITY** (`lib/utils/fileSniff.ts` magic-byte sniffing + 10MB server-side, pre-charge pre-storage; sniffed MIME to storage, never client-claimed) · **DEAD DOCUMENT-MATCHING FLOW REMOVED** (mismatch UI, confirm-scope route, SLA-paused labels, scope FAQ; brands_from_ocr out of the client payload; DB columns kept pending founder ruling) · documentPack false self-deletion comment corrected.

## 2. LIVE STATE (all verified, not assumed)

- **DB — every migration applied and read-back-verified** (all via MCP on explicit per-instance founder authorization; the founder-runs-prod law STANDS as the default): `20260730` admin batch · `20260731` brand_asins · `20260801` raise_credits RPC (founder-run himself) · `20260802` invitations+assignments · `20260803` three-role CHECK · `20260807` four-plan CHECKs. Migrations dir ↔ live schema fully in sync.
- **Stripe:** `STRIPE_PRICE_SINGLE_149` IS set (the audit confirmed checkout works). Portal plan-switching config: founder was instructed — **UNVERIFIED, confirm before relying on subscriber tier-switch.**
- **admin_permissions:** exactly 1 row (the founder, super_admin). admin_invitations/staff_client_assignments: 0 rows (correct fresh state).
- **`.env.local` has NO `SUPPORT_INBOX` and NO `RESEND_FROM`** — if Vercel matches, ALL admin alert emails are silently off (§5 config finding). CHECK FIRST THING.

## 3. THE RULED STATE (dated, all recorded in tracker/docs — this is the summary, not the source)

Ladder: **$99 (3 brands, 3 areas: identity/brand-risk/sourcing-logic, NO uploads) · $149 "Complete Report"[name=UNRULED placeholder] (3 brands, ALL 5 areas + category compliance, 2 uploads, 1 credit, standard SLA) · Growth $279 (5 brands, 5/cycle, all 5 areas, 2 uploads, NO category) · Scale $499 (5 brands, 12/cycle, + category compliance, 3-day SLA displayed)** · top-ups 3/$99, 6/$179 both subs · uploads cap NEVER in pricing copy · KEEPA_LIVE=false gates all ASIN collection · Verified/Assessed two-value client vocabulary (derivation locked; band prohibited) · Option A + riders 1&2 (upgrade credits) · Model 2 (capability follows active plan, stamped per case) · form-is-authoritative (documents corroborate entity/address ONLY; brand-absence-in-docs never negative — prohibited three ways in frozen Track 4) · refund policy LOCKED (ADMIN_FOUNDATIONS §9c verbatim; build = billing pass) · STOP-2 (operator picks tier/run) · STOP-3 (refunds dashboard-only) · Modes A/B = gate spec-first · ADR-008 demoted post-launch · hierarchy containment (two rules structural).

## 4. ENGINE-OUTPUT SHAPE (the UI thread's case-review design input — delivered in-chat 2026-08-02, key facts)

`buildVerdictViewModel` returns: engineComplete, verdict (recomputed+ceiled, full derivation), ceiling, assertion_advisories, executiveSummary (M9 + what_to_verify REMAPPED from M8), crossTrack (M4/M5/M7), tracks[] (per-dim: signal/band/score/evidence_items/reasoning_notes/brand_relationship_finding/boundary_notes/questions/unknowns/sourcing_logic), coverage, gaps, trace, certification_audits. Operators see RAW (signal/score/band); clients NEVER. Track 6 row lands in vm.tracks with dimension fallback literal "Track 6" (design must handle). `src_N` tags leak into narrative — stripping = the 1.2 gap, client-surface gate. Client projection = `lib/data/cases.ts` (allowlist, delivered-only, attempt-pinned, 3 mechanisms; a single named projection fn + report renderer = the gate's remaining build). Full report: the 2026-08-02 chat; lift into the docs-thread system explainer.

## 5. ⚠ THE GAP AUDIT (2026-08-07, read-only, conversation-only until now — the founder rules on the pre-design batch)

**Core spine VERIFIED COMPLETE end-to-end:** signup→onboarding→checkout→provisioning (new+existing Clerk user)→submit (validated, refund-on-failure)→pipeline→review→publish→outcome seed · top-ups · cancel/resume · Stripe portal session · invoices · support writes · dispute-rerun · $149 path sound (gating consistent at all 4 read sites, test-locked).

**BLOCKS DESIGN (recommended ~1-day pre-design batch):**
1. **Change-request feature fully built but ZERO entry points** (`app/(portal)/portal/cases/[id]/change/page.tsx` + API work; Help FAQ promises it; nothing links). Fix: button on delivered view + design brief.
2. **No delivery notification** — publish route notifies nobody; marketing says "Delivered to your email". Only 2 client emails exist in the whole product (support + change-request confirmations). Missing: delivery, submission-confirm, payment-failed, cancel-confirm. FOUNDER RULES email-on-delivery first.
3. **Billing "Buy another report" hardcodes single_99** — a $149 client gets charged $99 AND `activatePlan` downgrades their plan_type. + Dashboard plan card renders "$99/mo • 1 credits/month" for one-time buyers (PLAN_CADENCE exists, unused there).
4. **Growth "Upgrade to Scale →" dead-ends** at billing where subscribers have no upgrade control (real path = Stripe portal; nothing says so).
5. **Dead client-facing husks to excise before design restyles them:** Action Required tab/badge/amber rows (`awaiting_client` never written), "Queue #N" pill (`queue_position` no writer), legacy-questions branch (allowlist strips its key — `extractQuestions` always []).
6. **Marketing pricing copy vs shipped ladder:** sells 5 brands + 14-field doc review on $99 (now 3/none), "read-only" for Single (false), omits $149, still carries Keepa lines (pricing.ts:48,87 + homepage + the live Stripe Scale product description) · `/sample-report.pdf` 404s · footer Terms/Privacy/About are href="#".

**CAN FOLLOW (one post-design cleanup batch; none visible to a designer):**
- **Money/logic:** checkout has NO state guard (subscriber POSTing directly → SECOND subscription, or one-time purchase clobbers plan_type — UI hides, API doesn't) · credit-adjust silently no-ops on over-deduction yet returns ok:true + writes a false audit row; negative adjustments count as client "usage" · operator-house row inflates MRR/Revenue by phantom $499 + appears in client lists.
- **Gate consistency:** `view_cases` unchecked on /admin/cases + review + support PAGES (nav-only; scoping still applies) · `view_billing` unchecked on /admin/revenue · Publish/Override/Investigate buttons render for operators the API 403s (visible-but-refusing; only dispute-rerun is UI-gated) · `view_cases` checked by NO API route (near nav-only decoration).
- **Staff readiness (the day anyone is invited):** invitations + assignments are API-ONLY — no UI calls either; fresh admin/staff see ZERO clients until assignments are hand-called (highest-impact item) · operator-run cases invisible to the scoped staffer who ran them (house-row attribution) · operator-run skips the Trigger-9 legal alert (looks accidental) · admin layout comment stale (auth-only + TODO; requireAdmin covers, but a future page that forgets it gets nothing).
- **Config:** SUPPORT_INBOX/RESEND_FROM absent in .env.local → all sendAdminAlert paths (watchdog, pipeline failures, legal signals, degraded writes, outcome checkpoints) silently return no_admin_inbox; outcome-checkpoints STAMPS its 30/90 columns even when the email didn't send (nudge burned forever). Verify Vercel env.
- **Hygiene inventory:** two dev endpoints (validate-track1/validate-acquisition) run the REAL pipeline with real spend behind weak inconsistent gates ("remove once frozen") · review decision blobs in cases.internal_notes displayed nowhere (write-only reasons) · stripe_events.error un-surfaced; unhandled events marked processed · 4 case statuses never written (complete, intake_complete, qa_in_progress, approved) while ~20 sites branch on delivered||complete and FOUNDER_QUEUE_STATUSES lists the stale trio · last_active_at written once, orders Active Clients as if recency · billing_status 'trialling' styled-never-written / 'cancelling' written-never-styled · settings promises tax/billing fields reach invoices — nothing sends them to Stripe · clients cannot edit their own full_name · expired clients can open active case detail by URL (own data, gate inconsistency) · support type mapping lossy (escalation→technical) + dead defaultCaseId prop · accounting fetches Stripe payments never rendered; revenue eventCounts dead · checkout success/cancel params ignored by dashboard/billing (no confirmation moment) · /pricing CTAs discard the picked plan (all go to /sign-up) · case detail selects 7 fields it never displays · support queue status/admin_response/resolved_at have no writer (read-only ruling; columns dead) · dead tables: research_findings, supplier_cache, brand_cache, client_events, founder_notes, prompts, prompt_runs, qa_reviews, daily_case_count, category_flags, email_log, national_distributors, partner_program_brands, agencies; reports table+bucket (health check asserts a bucket nothing fills); admin_audit_log (one writer, zero readers); v_case_dashboard/v_admin_queue views + reset_client_credits dead · clients columns never written (max_files_per_submission, sla_days, deep_scenario, rollover_limit, referral_code, referred_by, tax_id_type, agency_id) · checkpoint_30/90 stamped but never rendered in outcome-panel.

## 6. FOUNDER LEDGER (his, current)

Rule the pre-design batch (§5 blocks-design; recommended) · rule email-on-delivery (§5.2) · **CHECK SUPPORT_INBOX + RESEND_FROM in Vercel env NOW** · verify Stripe portal plan-switch config · rule the $149 display name ("Complete Report" placeholder) · the named founder-run engine pass for the 7 frozen finding_certainty write sites (read-side derivation makes it non-urgent; backfill NOT recommended) · unused-schema removal ruling (OCR columns, dead tables list §5) · SLA ruling (1hr vs 3-5 days; sla_deadline never set — nothing displays true SLA today) · retention/legal pages · legal-banner render check (SQL in the 07-30 chat; probe found 0 triggering cases) · BUG-1 live check + canary first monthly run + untracked-folders ruling (standing since S-1F).

## 7. STANDING LAWS (deltas this arc; full set tracker §8 + HISTORY)

Founder-run script template is now: `npx tsx --conditions=react-server --tsconfig tsconfig.json --env-file=.env.local <script>` (server-only poison) · MCP prod writes happen ONLY on explicit per-instance founder authorization in chat (4 precedents this arc; describe-and-stop remains the default) · frozen-surface edits happen only founder-ordered and NAMED (2 precedents: tier gating lines, documentPack comment) · every new client-facing string joins the banned-language must-pass fixture SAME COMMIT (grown ~15 entries this arc) · productization fast-and-rough stands; the UI/UX thread follows `docs/UIUX_SESSION_PROMPT.md` (skill roster mandatory, layouts before code).

## 8. KEY IN-CODE FACTS A FRESH SESSION CANNOT GUESS

- Verified/Assessed derives in `lib/portal/certainty.ts`, applied ONLY in `getCaseFindings` (`lib/data/cases.ts`) — the stored column still says "unknown" everywhere and that is EXPECTED until the engine pass.
- `KEEPA_LIVE` in `lib/constants/plans.ts` is the single flip-point for all ASIN collection; `PLAN_ASIN_ELIGIBLE` = single_149 + scale_499; `brand_asins` rides `TrackContextWithIntake` (`lib/research/intakeExtras.ts`) through the pipeline payload spread.
- The grant core (`lib/auth/grants.ts`) is THE only path capabilities flow through (users API, invitations); `SUPER_ADMIN_ONLY_CAPS` are also stripped at READ time in `getOperator`.
- `navFor` + page gates + API gates all consume the same `can()` family — never add a second capability map.
- `planAcceptsUploads` (uploads.ts) is the $99 rule; the form's disabled state is presentation.
- Dev fixtures: AWI-2607-022 = `1e13b79e-09ce-4100-abf2-4b1333b449c5` (delivered, wired-engine); 021 = `2b359a6a-98f9-49c9-8f57-c19f4d8daaac`.
- Supabase project `mjkacjrrrmlwlwkienvq`; Vercel project `prj_fi4xRBMD0UdH3R0tdgqU1C2g3QT8`, team `team_tcDXxvsw5aBPtcsrlskgucjU`.
- lib/data/support.ts = the CLIENT's own-requests read; lib/data/adminSupport.ts = the admin queue. Never merge them (a Write-collision near-miss on 08-02 is why this line exists).
